# 闲鱼猎人「抢单策略」模块需求规格说明书

| 项 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 文档日期 | 2026-07-05 |
| 文档状态 | 初版（与详细设计 v2.0 对齐） |
| 所属项目 | 闲鱼猎人（XianyuHunter） |
| 所属菜单 | 配置管理 → 抢单策略 |
| 菜单标识 | `menu_registry.yaml` 中 `key=buyer`、`icon=ThunderboltOutlined`、`sort_order=20`、`category=config` |
| 关联路由 | 前端 `/config/buyer`；后端通用配置 `/api/config/*`、抢单相关 `/api/orders/*` |
| 关联文档 | [抢单策略-详细设计.md](抢单策略-详细设计.md)（v2.0/2026-07-05）、[抢单策略-操作手册.md](抢单策略-操作手册.md)（v1.0/2026-07-05） |
| 文档类型 | 需求规格说明书（SRS） |

> **本文档首段：继承 project_memory.md 中的硬约束。** 1. Web 进程必须使用 `with_browser=False` 模式；2. 认证白名单机制（不适用本模块）；3. 前端 fetch 必须含 `credentials: 'include'`；4. 401 必须返回 JSON；5. token 比较使用 `hmac.compare_digest()`；6. 敏感字段不日志；7. token 写失败触发 `logging.warning()`；8. 模块级 import；9. DB 索引齐全；10. COUNT 查询 CASE WHEN 聚合；11. WebView2 子进程 `CREATE_NEW_CONSOLE`；12. WebView2 `private_mode=False`。

---

## 0. 阶段交接声明

```
- 当前阶段：抢单策略 SRS 编写（v1.0） ✅ 已完成
- 下一阶段：抢单策略操作手册编写 + 三份文档一致性校验
- 下一阶段智能体：文档编写智能体
- 下一阶段技能：文档编写
- 交接上下文：
  · 本文档定义「配置管理 → 抢单策略」二级菜单完整需求 v1.0
  · 关键设计决策：1 个前端页面（BuyerStrategy）+ 复用通用配置接口 9 个 + 抢单相关 8 个 API 端点
  · 4 种任务级模式（AUTO/SEMI_AUTO/CONFIRM/NOTIFY_ONLY）
  · 2 个共享配置字段（pass_score/auto_buy_score，存储在 eval 配置块下）
  · 5 个运行时 dataclass 字段（BuyerConfig，未 UI 化）
  · 5 个 TaskConfig 字段（stop_on_first_buy/cooldown_after_buy 等，调度层运行时）
  · 订单状态机：pending_pay → takeover_pending → succeeded/failed/cancelled
  · 19 项新增功能：TakeoverTimeoutScheduler、manual-takeover、90 秒总体超时、F-16 依赖触发等
  · 与 v2.0 详细设计完全对齐，修正了 v1.0 详细设计的所有已知错误
```

---

## 1. 引言

### 1.1 编写目的

本需求规格说明书定义「配置管理 → 抢单策略」二级菜单的完整需求规格，作为前端/后端开发、测试与验收的唯一依据。

**目标读者**：
- 后端开发工程师：依据 §3-§6、§8、§10 进行模块实现与扩展
- 前端开发工程师：依据 §4、§9 进行页面开发
- 测试工程师：依据 §7、§11 编写测试用例与执行验收
- 项目经理：依据 §2、§11 进行范围与里程碑管理
- 智能客服 KB 训练：本文档与 [抢单策略-详细设计.md](抢单策略-详细设计.md) 共同作为 KB 检索素材

### 1.2 项目背景

闲鱼猎人（XianyuHunter）是一个闲鱼自动捡漏与抢单工具，已具备任务管理、商品采集、AI 评估、自动下单、多渠道通知等完整能力（详见 [requirements.md](../requirements.md)、[design.md](../design.md) 与 [概要设计文档.md](../概要设计文档.md)）。

随着系统运行规模扩大，**抢单策略相关的核心能力目前分散在多个模块（buyer_config / buyer / worker / evaluator / api_orders / takeover_timeout_scheduler）**，缺乏统一的配置入口，导致以下痛点：

| 痛点 | 现状 | 影响 |
|------|------|------|
| 分数阈值散落 | `pass_score` / `auto_buy_score` 存于 `eval` 配置块，但与"评估规则"页面共享 | 用户不清楚在哪个页面修改 |
| 任务模式说明缺失 | 4 种模式（AUTO/SEMI_AUTO/CONFIRM/NOTIFY_ONLY）的语义分散在代码 | 用户配置任务时易混淆 |
| 运行时参数不可见 | `BuyerConfig`（点击重试/超时/价格容差等）硬编码在代码 | 调参需改代码重启服务 |
| 安全护栏规则文案错误 | v1.0 文档错误描述"auto_buy_score ≥ 80 硬阈值"和"单日抢单上限" | 用户误以为是硬编码限制 |
| 接管超时机制不可见 | `TakeoverTimeoutScheduler` 5 分钟扫描、30 分钟超时仅在代码 | 用户不清楚接管订单何时过期 |
| 手动抢单入口缺失 | `manual-takeover` 接口已实现但页面无说明 | 用户不知道可手动触发抢单 |
| F-16 依赖触发不可见 | 订单 succeeded 后下游任务自动激活仅在代码 | 用户不清楚组合捡漏工作流 |

**解决方案**：通过统一的「抢单策略」配置页面 + 复用通用配置接口 9 个 + 抢单相关 8 个 API 端点，将分散能力整合为单一入口；同时修正 v1.0 详细设计的所有错误文案、补充 19 项新增功能说明，**让抢单策略可视化、可配置、可追溯**。

### 1.3 范围与约束

#### 1.3.1 包含范围（In-Scope）

- 配置字段管理：`eval.pass_score` / `eval.auto_buy_score`（共享配置块）
- 4 种任务级模式说明展示（AUTO/SEMI_AUTO/CONFIRM/NOTIFY_ONLY）
- 安全护栏规则展示与说明（含修正后的文案）
- Diff 预览 + 二次确认保存流程
- 通用配置接口 9 端点（GET/preview/save/share/export/import/version/rollback/restore-backup）
- 抢单相关接口 8 端点（list/get/delete/takeover/takeover_confirm/takeover_cancel/update_status/manual-takeover）
- F-16 下游依赖自动激活内部函数
- TakeoverTimeoutScheduler 后台调度（5 分钟扫描、30 分钟超时清理）
- 数值防御（clamp 函数 [0, 100]）
- 热更新机制（保存后立即生效，无需重启）

#### 1.3.2 排除范围（Out-of-Scope）

- 不实现 BuyerConfig 运行时参数的 UI 化（当前硬编码，待后续版本接入"高级设置"）
- 不实现 TakeoverTimeoutScheduler 的 UI 配置（参数固定 5/30 分钟，待 UI 化）
- 不实现任务级模式切换（模式在"任务管理"页面按任务配置，本页面仅展示说明）
- 不实现独立 REST 路由（复用通用 `/api/config` 接口）
- 不实现独立数据库表（共享 `eval` 配置块，存储在 YAML）
- 不实现评估算法配置（在"评估规则"页面配置 weights/thresholds）

#### 1.3.3 硬约束

继承 [project_memory.md](c:/Users/hspcadmin/.trae-cn/memory/projects/-d-code-otherProjects-17-xianyu/project_memory.md) 中的项目硬约束，并显式映射到本模块实现：

| 硬约束 | 本模块适用性 | 实现要点 |
|--------|--------------|----------|
| Web 进程使用 `with_browser=False` 模式 | 适用 | Web 进程本身不启浏览器；抢单需 `XH_WITH_SCHEDULER=1` 模式注入 buyer |
| 认证白名单（`/api/auth/cookie` 等） | 不适用 | `/api/config/*` 与 `/api/orders/*` **必须**走 `auth.py` 认证 |
| 前端 fetch 必须含 `credentials: 'include'` | 适用 | `frontend/src/api/config.ts` 全量沿用 `client` 默认配置 |
| 401 响应必须返回 JSON `{"detail":"Unauthorized"}` | 适用 | `api_config.py` / `api_orders.py` 路由层统一异常处理 |
| 后端 token 比较使用 `hmac.compare_digest()` | 适用 | 通用配置接口认证遵循 |
| 敏感字段（token 长度等）不日志 | 适用 | §8.5 日志规范：仅记录 `user_id / path / op` |
| token 写失败触发 `logging.warning()` | 适用 | 配置写盘失败时 `logger.warning` |
| 缺失 import 必须 module-level | 适用 | `takeover_timeout_scheduler.py` 全部模块级 import |
| DB 必须在 `task_id/seller_id/...` 加索引 | 适用 | `orders` 表已有 `task_id`/`item_id`/`status` 索引 |
| COUNT 查询用 CASE WHEN 聚合 | 不适用 | 本模块不涉及 COUNT 聚合查询 |
| WebView2 子进程使用 `CREATE_NEW_CONSOLE` | 适用 | 抢单流程依赖浏览器进程 |
| WebView2 `webview.start()` 设 `private_mode=False` + 独立 `storage_path` | 适用 | buyer 持久化 cookie |
| KBManager 排除 `web/static/` 等目录 | 不适用 | 本模块不涉及 KB 索引 |
| `local_embedding.py` HF_ENDPOINT 设置 | 不适用 | 本模块不涉及 embedding |
| EmbeddingService 本地后端 | 不适用 | 同上 |
| `run_migrations` 各块独立 try/except | 适用 | orders 表迁移块独立 try/except |
| 启动钩子异常 `logger.exception()` | 适用 | `startup.py` 调度器初始化失败时打印完整 traceback |

### 1.4 术语与缩写

| 术语 | 全称 | 说明 |
|------|------|------|
| EvalConfig | Evaluation Config | 评估配置 Pydantic 模型，含 pass_score/auto_buy_score |
| BuyerConfig | Buyer Config | 抢单运行时 dataclass，含点击重试/超时/价格容差等 |
| TaskConfig | Task Config | 任务运行时配置（不持久化），由调度层构造 |
| TaskMode | Task Mode | 任务执行模式枚举（4 种：auto/semi_auto/confirm/notify） |
| OrderStatus | Order Status | 订单状态枚举（4 核心值 + 2 扩展值） |
| pass_score | Pass Score | 通过分数（≥此值推送通知），默认 60 |
| auto_buy_score | Auto Buy Score | 自动抢单分数（≥此值且 AUTO 模式才自动拍下），默认 80 |
| pass_score ≤ auto_buy_score | — | Pydantic `_check_score_order` 校验器强制的顺序约束 |
| clamp | Clamp | 前端数值防御函数，确保 [0, 100] 范围 |
| stop_on_first_buy | Stop On First Buy | 抢到 1 单后停止本轮（防刷单），默认 True |
| cooldown_after_buy | Cooldown After Buy | 拍下后冷却秒数，默认 30.0 |
| click_retry_times | Click Retry Times | "立即购买"按钮点击重试次数，默认 2 |
| click_retry_interval | Click Retry Interval | 两次点击退避秒数，默认 1.0 |
| confirm_button_timeout | Confirm Button Timeout | 等待"提交订单"按钮超时，默认 10.0s |
| price_tolerance | Price Tolerance | 拍下价相对评估价的允许偏差，默认 0.05（5%） |
| min_interval_between_orders | Min Interval Between Orders | 两次落单最小间隔，默认 0.0 |
| takeover_pending | Takeover Pending | 订单中间态：等待人工接管支付 |
| succeeded | Succeeded | 抢单成功（与 paid 等价，历史遗留） |
| pending_pay | Pending Pay | 已拍下待支付，订单初始状态 |
| TakeoverTimeoutScheduler | Takeover Timeout Scheduler | 接管超时清理调度器（5 分钟扫描、30 分钟超时） |
| TAKEOVER_TIMEOUT_MIN | Takeover Timeout Minutes | 接管超时阈值（30 分钟），对应闲鱼待付款自动关闭 |
| _DEFAULT_SCAN_INTERVAL_MIN | Default Scan Interval Minutes | 超时扫描间隔（5 分钟） |
| manual-takeover | Manual Takeover | 手动抢单接口，突破纯自动模式限制 |
| F-16 | Feature 16 | 订单 succeeded 后下游 paused 任务自动激活 |
| _trigger_dependent_tasks | Trigger Dependent Tasks | F-16 同步函数，激活下游 paused 任务 |
| dep_triggered | Dep Triggered | 依赖触发 events 表 stage 标识 |
| BuyerError | Buyer Error | 落单流程异常基类 |
| ButtonNotFoundError | Button Not Found Error | 找不到「立即购买」按钮（页面结构变化） |
| ItemSoldError | Item Sold Error | 商品已售出（返回 409） |
| OutOfStockError | Out Of Stock Error | 商品已下架/无库存 |
| PriceMismatchError | Price Mismatch Error | 价格偏差过大（超 5%） |
| LRU | LRU Cache | 内存幂等缓存（maxsize=10000） |
| browser_lock | Browser Lock | 浏览器并发操作互斥锁（优先级：high/normal/low） |
| useConfigStore | Use Config Store | 前端配置状态管理 hook |
| dry_run | Dry Run | 预览改动不写盘 |
| DiffChange | Diff Change | 配置变更项（path/op/old/new） |

### 1.5 参考资料

| 文档 | 路径 | 用途 |
|------|------|------|
| 需求文档 v1.0 | [../requirements.md](../requirements.md) | 系统整体需求基线 |
| 概要设计文档 v2.0 | [概要设计文档.md](../概要设计文档.md) | 36 个 API 路由、11 张数据表、技术选型 |
| 技术设计说明书 | [../design.md](../design.md) | 分层架构、Evaluator 设计、状态机 |
| 抢单策略 详细设计 v2.0 | [抢单策略-详细设计.md](抢单策略-详细设计.md) | 本模块技术设计、代码位置、FAQ |
| 抢单策略 操作手册 v1.0 | [抢单策略-操作手册.md](抢单策略-操作手册.md) | 用户操作指南 |
| 任务管理 SRS | [../02-数据查看/任务管理-需求规格.md](../02-数据查看/任务管理-需求规格.md) | SRS 模板参考（12 章节完整版） |
| 项目硬约束 | [project_memory.md](c:/Users/hspcadmin/.trae-cn/memory/projects/-d-code-otherProjects-17-xianyu/project_memory.md) | 必须继承的硬约束清单 |
| 菜单元数据 | [../../config/menu_registry.yaml](../../config/menu_registry.yaml) | `key=buyer`、`sort_order=20` |
| 通用配置后端 | [../../src/xianyu_hunter/web/routes/api_config.py](../../src/xianyu_hunter/web/routes/api_config.py) | 9 个通用配置端点 |
| 抢单订单后端 | [../../src/xianyu_hunter/web/routes/api_orders.py](../../src/xianyu_hunter/web/routes/api_orders.py) | 8 个抢单相关端点 + F-16 + manual-takeover |
| 接管超时调度器 | [../../src/xianyu_hunter/modules/takeover_timeout_scheduler.py](../../src/xianyu_hunter/modules/takeover_timeout_scheduler.py) | TakeoverTimeoutScheduler |
| 配置模型 | [../../src/xianyu_hunter/infra/yaml_config.py](../../src/xianyu_hunter/infra/yaml_config.py) | EvalConfig Pydantic 模型 |
| 抢单运行时 | [../../src/xianyu_hunter/modules/buyer_config.py](../../src/xianyu_hunter/modules/buyer_config.py) | BuyerConfig dataclass |
| 抢单核心 | [../../src/xianyu_hunter/modules/buyer.py](../../src/xianyu_hunter/modules/buyer.py) | 90 秒超时、LRU 幂等、_do_buy 流程 |
| Worker | [../../src/xianyu_hunter/modules/worker.py](../../src/xianyu_hunter/modules/worker.py) | _should_buy/_in_cooldown/_attempt_buy |
| 评估器 | [../../src/xianyu_hunter/modules/evaluator.py](../../src/xianyu_hunter/modules/evaluator.py) | _score_to_risk 风险等级映射 |
| 任务领域 | [../../src/xianyu_hunter/domain/task.py](../../src/xianyu_hunter/domain/task.py) | TaskMode/TaskConfig |
| 订单领域 | [../../src/xianyu_hunter/domain/order.py](../../src/xianyu_hunter/domain/order.py) | OrderStatus/BuyerError 异常体系 |
| 订单仓储 | [../../src/xianyu_hunter/infra/repo_orders.py](../../src/xianyu_hunter/infra/repo_orders.py) | expire_takeover_pending_orders |
| 前端页面 | [../../frontend/src/pages/Config/BuyerStrategy.tsx](../../frontend/src/pages/Config/BuyerStrategy.tsx) | BuyerStrategy 组件 |
| 配置状态管理 | [../../frontend/src/stores/configStore.ts](../../frontend/src/stores/configStore.ts) | useConfigStore hook |

---

## 2. 总体描述

### 2.1 产品定位

「抢单策略」是闲鱼猎人系统的**抢单行为配置入口**，定位为：

> 通过分数阈值（`pass_score` / `auto_buy_score`）+ 4 种任务级执行模式 + 安全护栏（90 秒超时、价格容差、冷却期、接管超时清理）+ 人工接管流程，控制 auto 模式下的抢单行为，防止误拍与资金锁定风险。

**核心价值**：
1. **配置可视化**：分数阈值、任务模式说明、安全护栏规则全部在单一页面展示
2. **复用避免冗余**：`pass_score` / `auto_buy_score` 存于 `eval` 配置块，与"评估规则"页面共享
3. **4 种模式全覆盖**：从"仅通知"到"全自动"的全场景模式说明
4. **安全护栏完善**：90 秒总体超时、价格容差校验、LRU+DB 双重幂等、stop_on_first_buy、cooldown_after_buy、接管超时清理
5. **接管流程闭环**：pending_pay → takeover_pending → succeeded/failed/cancelled 状态机 + 30 分钟超时清理
6. **手动抢单突破**：manual-takeover 接口让用户在评估明细页主动触发
7. **F-16 依赖激活**：订单 succeeded 后下游 paused 任务自动激活
8. **热更新**：保存后立即生效，无需重启服务
9. **二次确认 + Diff 预览**：防止误操作（如把 auto_buy_score 从 85 改成 50）
10. **Pydantic + dataclass 双层架构**：配置层（EvalConfig）持久化 YAML + 运行时层（BuyerConfig）默认值实例化

### 2.2 用户角色与特征

| 角色 | 特征 | 使用场景 | 关注重点 |
|------|------|----------|----------|
| 保守用户 | 资金安全优先，不希望自动拍下 | 配置 pass_score=70, auto_buy_score=85，任务用 notify/confirm 模式 | 安全护栏、模式说明、分数阈值推荐值 |
| 平衡用户 | 兼顾效率与安全，希望高置信度商品自动拍 | 配置 pass_score=60, auto_buy_score=80（默认值），任务用 semi_auto 模式 | 分数阈值、接管流程、手动抢单 |
| 激进用户 | 抢时间，希望低分商品也能自动拍 | 配置 pass_score=50, auto_buy_score=75，任务用 auto 模式 | 风险等级映射、冷却期、stop_on_first_buy |
| 人工接管用户 | 已有 pending_pay 订单，需在闲鱼 App 支付 | 在抢单记录页点击"接管"，30 分钟内完成支付 | 接管超时、状态机白名单 |
| 调试运维 | 排查抢单失败原因 | 查看 BuyerConfig 参数、BuyerError 异常体系 | 异常类型、90 秒超时、价格容差 |
| 组合捡漏用户 | 配置 F-16 依赖链，上游抢单成功激活下游 | 在任务管理配置依赖，等待自动激活 | F-16 触发条件、dep_triggered 事件 |

### 2.3 运行环境

| 项 | 要求 |
|---|---|
| 操作系统 | Windows 10/11（主），Linux/macOS 需兼容 |
| Python | 3.11+ |
| Node.js | 18+（仅前端构建） |
| 后端框架 | FastAPI（已集成） |
| 前端框架 | React 18 + TypeScript + Ant Design 5（已集成） |
| 抢单模式 | `XH_WITH_SCHEDULER=1` 启动时启用 buyer 注入，与 Web 共享 Container |
| 数据库 | 复用现有 SQLite（orders / events / items 表） |
| 配置存储 | 复用现有 YAML（`eval.pass_score` / `eval.auto_buy_score`） |

### 2.4 设计约束

1. **集成部署**：作为 `frontend/src/pages/Config/BuyerStrategy.tsx` + 复用 `api_config.py` / `api_orders.py` 路由集成到现有系统，不独立部署
2. **复用 Container 单例**：所有端点通过 `Depends(get_container)` 访问 Container，保证状态一致
3. **写操作 POST，读操作 GET**：与现有 REST 规范对齐
4. **异步操作 async 路由**：`POST /api/config/save`、`POST /api/orders/manual-takeover` 使用 async 路由
5. **前端布局对齐设计系统**：分数阈值 Card + 任务级模式 Card + 安全护栏 Card + Diff 预览 Modal
6. **状态管理统一**：前端通过 `useConfigStore` 全局管理配置状态，与"评估规则"页面共享
7. **多用户隔离**：通用配置接口通过 `request.state.user_id` 鉴权
8. **保存主色**：使用 `#FF6200`（闲鱼橙）突出抢单操作的重要性
9. **数值防御**：`clamp` 函数确保分数在 [0, 100] 范围
10. **二次确认**：保存前弹出 Diff 预览 Modal

### 2.5 假设与依赖

**假设**：
- 用户已部署闲鱼猎人 v2.x+ 并能访问 `MainLayout` 侧边栏
- 通用配置接口 `/api/config` 已实现并稳定
- YAML 配置文件已初始化（含 `eval` 配置块）
- 抢单相关接口 `/api/orders/*` 已实现

**依赖**：
- 依赖现有 `auth.py` 认证中间件
- 依赖现有 `Container`（注入 `repo`/`buyer`/`browser_lock`）
- 依赖现有 `useConfigStore` 全局状态管理
- 依赖现有 `menu_registry.yaml` 菜单注册（已配置 `key=buyer`）
- 依赖现有 `TaskMode` / `OrderStatus` 领域枚举
- 依赖现有 `TakeoverTimeoutScheduler` 后台调度（服务启动时初始化）
- 新增无外部依赖

---

## 3. 总体架构设计

### 3.1 模块架构图

```mermaid
graph TB
    subgraph "前端 Frontend"
        BS[BuyerStrategy.tsx<br/>分数阈值 + 模式说明 + 安全护栏]
        CS[useConfigStore<br/>load/update/previewSave/confirmSave]
        CST[configStore.ts<br/>DiffChange 类型]
    end

    subgraph "Web 层 web/routes/"
        AC[api_config.py<br/>/api/config 9 端点<br/>通用配置读写]
        AO[api_orders.py<br/>/api/orders 8 端点<br/>抢单相关 + F-16 + manual-takeover]
    end

    subgraph "业务模块层 modules/"
        WK[TaskWorker<br/>_should_buy/_in_cooldown/_attempt_buy]
        BY[Buyer<br/>buy + 90s 超时 + LRU 幂等]
        EV[Evaluator<br/>_score_to_risk 风险映射]
        TT[TakeoverTimeoutScheduler<br/>5min 扫描 + 30min 超时]
    end

    subgraph "领域层 domain/"
        TASK[TaskMode 4 种<br/>TaskConfig dataclass]
        ORD[OrderStatus 4 核心 + 2 扩展<br/>BuyerError 异常体系]
    end

    subgraph "基础设施层 infra/"
        YC[yaml_config.py<br/>EvalConfig Pydantic + _check_score_order]
        RO[repo_orders.py<br/>expire_takeover_pending_orders]
        DB[(SQLite<br/>orders / events / items)]
        YML[(config.yaml<br/>eval.pass_score / eval.auto_buy_score)]
    end

    BS --> CS
    CS --> AC
    BS -.共享.-> EV
    AO --> BY
    AO --> RO
    AO --> TT
    WK --> BY
    WK --> EV
    BY --> RO
    TT --> RO
    RO --> DB
    AC --> YC
    YC --> YML

    style BS fill:#e6f7ff,stroke:#1890ff
    style AC fill:#f6ffed,stroke:#52c41a
    style AO fill:#fff7e6,stroke:#fa8c16
    style TT fill:#fff1f0,stroke:#f5222d
```

### 3.2 数据流程图

```mermaid
flowchart TB
    subgraph "用户操作"
        U1[进入抢单策略页面]
        U2[修改分数阈值]
        U3[保存配置]
        U4[接管订单]
        U5[手动抢单]
    end

    subgraph "前端"
        L1[useConfigStore.load<br/>拉取 eval 配置]
        L2[update 函数<br/>patch 合并更新]
        L3[previewSave<br/>dry_run 预览]
        L4[confirmSave<br/>写盘 + reload_config]
    end

    subgraph "后端配置接口"
        C1[GET /api/config<br/>读取 eval 配置]
        C2[POST /api/config/preview<br/>dry_run + 校验]
        C3[POST /api/config/save<br/>写盘 + 多版本备份]
    end

    subgraph "后端抢单接口"
        O1[POST /api/orders/{id}/takeover<br/>发起接管]
        O2[POST /api/orders/{id}/takeover/confirm<br/>确认支付]
        O3[POST /api/orders/manual-takeover<br/>手动抢单]
        O4[_trigger_dependent_tasks<br/>F-16 同步函数]
    end

    subgraph "调度器"
        S1[TakeoverTimeoutScheduler<br/>5min 扫描]
        S2[expire_takeover_pending_orders<br/>30min 超时清理]
    end

    subgraph "数据"
        D1[(config.yaml<br/>eval 配置块)]
        D2[(orders 表<br/>订单状态)]
        D3[(events 表<br/>dep_triggered)]
    end

    U1 --> L1
    L1 --> C1
    C1 --> D1

    U2 --> L2
    U3 --> L3
    L3 --> C2
    L3 --> L4
    L4 --> C3
    C3 --> D1

    U4 --> O1
    U4 --> O2
    O1 --> D2
    O2 --> D2
    O2 --> O4
    O4 --> D3

    U5 --> O3
    O3 --> D2

    S1 --> S2
    S2 --> D2

    style L1 fill:#e6f7ff
    style S2 fill:#fff1f0
    style O4 fill:#fff7e6
```

### 3.3 关键时序图

#### 3.3.1 配置保存时序（dry_run + 二次确认）

```mermaid
sequenceDiagram
    participant F as 前端
    participant S as useConfigStore
    participant A as api_config.py
    participant Y as yaml_config.py
    participant DB as SQLite

    F->>S: 点击保存按钮
    S->>A: POST /api/config/preview {payload, dry_run=true}
    A->>Y: Pydantic 校验 _check_score_order
    alt pass_score > auto_buy_score
        A-->>S: 422 校验错误
        S-->>F: 显示红色 Alert
    else 校验通过
        A->>A: 计算变更 diffs
        A-->>S: {diffs, validation:{ok}, new_yaml}
        S-->>F: 弹出 Diff 预览 Modal
        F->>F: 用户确认变更
        F->>S: 点击确认保存
        S->>A: POST /api/config/save {payload, dry_run=false}
        A->>Y: 写盘 config.yaml
        A->>Y: reload_config() 重置单例
        A->>DB: 备份版本 + 清理旧备份
        A-->>S: {ok, saved_at, backup}
        S-->>F: message.success("抢单策略已保存")
    end
```

#### 3.3.2 接管流程时序（pending_pay → takeover_pending → succeeded）

```mermaid
sequenceDiagram
    participant F as 前端
    participant A as api_orders.py
    participant R as repo
    participant S as TakeoverTimeoutScheduler
    participant DB as SQLite

    Note over F: 订单状态 pending_pay
    F->>A: POST /api/orders/{id}/takeover
    A->>A: 校验状态 == pending_pay
    alt 状态非 pending_pay
        A-->>F: 400 "仅 pending_pay 可被接管"
    else 校验通过
        A->>R: update status=takeover_pending, confirmed_at=now
        A-->>F: {ok, id, status:"takeover_pending"}
        F->>F: 启动 30 分钟倒计时
    end

    Note over F: 用户在闲鱼 App 支付
    F->>A: POST /api/orders/{id}/takeover/confirm
    A->>R: update status=succeeded
    A->>A: _trigger_dependent_tasks(order)
    alt 有下游 paused 任务
        A->>R: update_task_status(downstream, running)
        A->>R: save_event(stage="dep_triggered")
    end
    A-->>F: {ok, id, status:"succeeded"}

    Note over S: 5 分钟扫描一次
    S->>R: expire_takeover_pending_orders(30)
    R->>DB: SELECT takeover_pending 且 confirmed_at < now-30min
    alt 存在超时订单
        R->>DB: UPDATE status=failed, confirmed_at=NULL
        R-->>S: 返回超时订单列表
    end
```

#### 3.3.3 手动抢单时序（manual-takeover）

```mermaid
sequenceDiagram
    participant F as 前端
    participant A as api_orders.py
    participant L as BrowserLock
    participant B as Buyer
    participant R as repo

    F->>A: POST /api/orders/manual-takeover {task_id, item_id}
    A->>A: _ensure_takeover_prerequisites
    alt buyer 未注入
        A-->>F: 503 "抢单功能未启用"
    else Cookie 失效
        A-->>F: 403 "闲鱼登录已过期"
    end
    A->>R: _check_existing_takeover_order 幂等检查
    alt 已存在订单
        A-->>F: {ok, status:"skipped_duplicate"}
    else 不存在
        A->>L: acquire(priority="high", timeout=10s)
        alt 锁获取失败
            A-->>F: 503 "系统正在执行后台浏览器任务"
        else 锁获取成功
            A->>B: container.buyer.buy(task_id, item_id, detail)
            B->>B: 90s asyncio.wait_for(_do_buy)
            alt ItemSoldError
                B-->>A: ItemSoldError
                A-->>F: 409 "商品已售出"
            else 成功
                B->>R: upsert_order(status=pending_pay)
                B-->>A: BuyResult(outcome=SUCCESS)
                A-->>F: {ok, status:"success", order_id}
            end
            A->>L: release()
        end
    end
```

### 3.4 模块分层与职责

遵循项目现有 DDD 分层架构（见 [概要设计文档](../概要设计文档.md) §3）：

| 层级 | 模块 | 职责 |
|------|------|------|
| **Web 层** | `api_config.py` | 通用配置 9 端点（GET/preview/save/share/export/import/version/rollback/restore-backup） |
| | `api_orders.py` | 抢单相关 8 端点 + F-16 同步函数 `_trigger_dependent_tasks` + manual-takeover |
| **业务模块层** | `worker.py` | TaskWorker._should_buy / _in_cooldown / _attempt_buy（按模式判断 + 冷却期 + auto_buy_score 判定） |
| | `buyer.py` | Buyer.buy + 90s 超时 + LRU 幂等 + DB 幂等兜底 + 价格校验 + 异常体系 |
| | `evaluator.py` | Evaluator._score_to_risk 风险等级映射（使用 thresholds.auto_buy_score，非硬编码） |
| | `takeover_timeout_scheduler.py` | TakeoverTimeoutScheduler 5min 扫描 + 30min 超时清理 |
| **领域层** | `domain/task.py` | TaskMode 枚举（4 种）+ TaskConfig dataclass |
| | `domain/order.py` | OrderStatus 枚举（4 核心 + 2 扩展）+ BuyerError 异常体系 |
| **基础设施层** | `infra/yaml_config.py` | EvalConfig Pydantic 模型 + _check_score_order 校验器 |
| | `infra/repo_orders.py` | upsert_order / get_order / list_orders / find_order_by_task_item / expire_takeover_pending_orders |
| | `infra/db_models.py` | OrderRow（订单表 ORM） |
| **前端** | `pages/Config/BuyerStrategy.tsx` | 抢单策略页面（分数阈值 Card + 模式 Card + 安全护栏 Card + Diff Modal） |
| | `stores/configStore.ts` | useConfigStore hook（load/update/previewSave/confirmSave/reset） |

---

## 4. 功能需求

### FR-1 分数阈值配置

**FR-1.1** `pass_score` 配置
- 字段位置：`eval.pass_score`（与"评估规则"页面共享）
- 类型：`int`，范围 [0, 100]
- 默认值：`60`
- 含义：≥此值的商品会推送通知给用户
- 前端控件：`InputNumber`，min=0, max=100, addonAfter="分"
- 数值防御：`clamp(v)` 函数处理 `null/NaN/undefined` → 0，超出 [0, 100] 截断

**FR-1.2** `auto_buy_score` 配置
- 字段位置：`eval.auto_buy_score`（与"评估规则"页面共享）
- 类型：`int`，范围 [0, 100]
- 默认值：`80`
- 含义：≥此值且任务为 `AUTO` 模式才自动拍下
- 前端控件：`InputNumber`，min=0, max=100, addonAfter="分"
- **重要澄清**：默认值 80 是**可配置项**，不是硬编码阈值。worker.py 通过 `eval_cfg.auto_buy_score if eval_cfg else 80` 读取

**FR-1.3** `_check_score_order` Pydantic 校验器
- 强制约束：`pass_score <= auto_buy_score`
- 违反时抛 `ValueError`
- 前端实时检测：`scoreOrderError` 变量，显示红色 Alert 提示

**FR-1.4** 字段共享机制
- `pass_score` / `auto_buy_score` 存储在 `eval` 配置块下
- 与"评估规则"页面**共享**，在任一页面修改都会影响另一页面
- 通过 `useConfigStore` 全局状态管理，避免配置冗余

### FR-2 任务级模式说明

**FR-2.1** 4 种任务级模式（`TaskMode` 枚举）：

| 模式 | 枚举值 | 字符串值 | 行为 | 风险等级 |
|------|--------|----------|------|----------|
| 全自动 | `AUTO` | `auto` | 评估通过且评分 ≥ `auto_buy_score` 且风险 LOW 时自动拍下 | 高风险 |
| 半自动 | `SEMI_AUTO` | `semi_auto` | 推送通知，用户确认后抢单 | 中风险 |
| 需确认 | `CONFIRM` | `confirm` | 仅推送通知，不抢单（默认最保守） | 推荐 |
| 仅通知 | `NOTIFY_ONLY` | `notify` | 仅推送通知 | 最安全 |

**FR-2.2** 模式配置入口
- 模式是**任务级配置**，在"任务管理"页面按任务独立设置
- 本页面（抢单策略）仅展示说明，不直接配置
- 修改后**下次调度执行时生效**，不影响正在执行的任务

**FR-2.3** 前端模式说明 Card
- 展示 3 个模式卡片（notify/confirm/auto），每个卡片含图标 + 标签 + 描述
- 配色：notify=蓝色 #1677ff（安全）、confirm=黄色 #faad14（注意）、auto=橙色 #FF6200（危险）
- **前端待修正项**：`MODES` 数组当前只有 3 个条目，需补充 `semi_auto` 模式卡片

**FR-2.4** `_should_buy` 判定逻辑（worker.py）
- `AUTO` 模式 → `True`（执行真拍）
- `NOTIFY_ONLY` 模式 → `False`
- `CONFIRM` / `SEMI_AUTO` 模式 → `False`（不在 Worker 自动拍，由用户在 App 内确认）

### FR-3 安全护栏

**FR-3.1** 安全护栏规则展示
- 页面"安全护栏" Card 列出实际生效的护栏规则
- **v1.0 文档错误需修正**：
  - "auto_buy_score ≥ 80 才启用 auto 模式（硬编码）" → 实际为**可配置默认值 80**
  - "单日抢单上限：在 buyer_config.py 中配置（待 UI 化）" → 实际 `BuyerConfig` 中**无此字段**

**FR-3.2** 实际安全护栏机制（9 项）：

| 序号 | 护栏 | 默认值 | 说明 |
|------|------|--------|------|
| 1 | 90 秒总体超时 | `90.0s` | `asyncio.wait_for(timeout=90.0)`，防止浏览器操作无限阻塞 |
| 2 | 价格容差校验 | `0.05`（5%） | `BuyerConfig.price_tolerance`，防卖家临时涨价 |
| 3 | LRU 内存幂等 | `maxsize=10000` | `LRUDict[tuple[str,str], None]` 防进程内重复 |
| 4 | DB 幂等兜底 | — | `find_order_by_task_item` 防跨进程重启后重复 |
| 5 | stop_on_first_buy | `True` | 抢到 1 单后停止本轮（防刷单） |
| 6 | cooldown_after_buy | `30.0s` | 拍下后冷却期（防连点） |
| 7 | 接管超时清理 | `30min` | TakeoverTimeoutScheduler 5min 扫描，超时流转到 failed |
| 8 | buyer 注入态校验 | — | `with_browser=False` 模式下拒绝手动抢单 |
| 9 | 登录态校验 | — | Cookie 失效时拒绝抢单 |
| 10 | 浏览器锁 | `priority="high"` | `browser_lock.acquire` 串行化浏览器操作 |

**FR-3.3** 警告 Alert
- "谨慎使用 auto 模式"：自动拍下会立即锁定资金，请确认已充分测试
- 类型：`warning`，图标：`WarningOutlined`

### FR-4 通用配置接口（9 端点）

**FR-4.1** `GET /api/config` 读取配置
- 响应：完整配置对象，含 `eval: {pass_score, auto_buy_score, ...}`
- 本页面仅消费 `eval.pass_score` 和 `eval.auto_buy_score` 字段

**FR-4.2** `POST /api/config/preview` 预览改动
- 请求体：`{payload: {eval: {pass_score, auto_buy_score}}, dry_run: bool}`
- 响应：`{diffs: [{path, op, old, new}], validation: {ok, errors}, new_yaml}`
- Pydantic 校验：`_check_score_order` 强制 `pass_score <= auto_buy_score`

**FR-4.3** `POST /api/config/save` 保存配置
- 请求体：`{payload: {eval: {...}}, dry_run: bool}`
- 响应：`{ok, dry_run, saved_at, diffs, backup: {filename, pruned}, restart_required}`
- 写盘 + `reload_config()` 重置单例 + 多版本备份
- `restart_required`：`BuyerConfig` 运行时参数变更需重启（当前未 UI 化）

**FR-4.4** `GET /api/config/share` 获取可分享的脱敏配置
- 响应：`{yaml: str}`（脱敏后的 YAML 字符串）

**FR-4.5** `GET /api/config/export` 导出 YAML 文件
- 响应：文件下载

**FR-4.6** `POST /api/config/import` 导入 YAML 配置
- 请求体：`{yaml: str}`
- 响应：`{ok, saved_at}`

**FR-4.7** `GET /api/config/version` 列出备份版本
- 响应：`{items: [{filename, saved_at}]}`

**FR-4.8** `POST /api/config/rollback` 回滚到指定版本
- 请求体：`{filename: str}`
- 响应：`{ok, saved_at}`

**FR-4.9** `POST /api/config/restore-backup` 从备份恢复
- 请求体：`{filename: str}`
- 响应：`{ok}`

### FR-5 抢单相关接口（8 端点）

**FR-5.1** `GET /api/orders` 订单列表
- Query：`status?`、`task_id?`、`item_id?`、`limit?=50`、`offset?=0`
- 响应：`{items[], count, total, limit, offset}`

**FR-5.2** `GET /api/orders/{order_id}` 订单详情
- 响应：完整 OrderRow

**FR-5.3** `DELETE /api/orders/{order_id}` 删除订单
- 响应：`{ok, id}`

**FR-5.4** `POST /api/orders/{order_id}/takeover` 发起人工接管
- 状态机白名单：仅 `pending_pay` 可被接管
- 拒绝 `takeover_pending → takeover_pending`：避免用户无限点击重置 30 分钟倒计时
- 响应：`{ok, id, status: "takeover_pending"}`

**FR-5.5** `POST /api/orders/{order_id}/takeover/confirm` 确认支付完成
- 状态流转：`takeover_pending → succeeded`
- 触发 F-16：`_trigger_dependent_tasks(order)` 激活下游 paused 任务
- 响应：`{ok, id, status: "succeeded"}`

**FR-5.6** `POST /api/orders/{order_id}/takeover/cancel` 取消接管
- 状态流转：`takeover_pending → pending_pay`（回退，清空 confirmed_at）
- 响应：`{ok, id, status: "pending_pay"}`

**FR-5.7** `PATCH /api/orders/{order_id}/status` 修改订单状态
- 触发 F-16：当状态变为 `succeeded` 时调用 `_trigger_dependent_tasks`
- 响应：`{ok, id, status}`

**FR-5.8** `POST /api/orders/manual-takeover` 手动抢单
- 请求体：`{task_id, item_id, ...}`
- 前置校验：buyer 注入态 + 闲鱼登录态
- 浏览器锁：`priority="high", timeout=10.0`
- 异常处理：`ItemSoldError` → 409，其他 → 500
- 响应：`{ok, status: "success"|"skipped_duplicate", order_id?}`

### FR-6 F-16 下游依赖自动激活

**FR-6.1** 触发条件
1. 订单状态变为 `succeeded`（通过 `takeover_confirm` 或 `update_order_status`）
2. 订单关联了一个 `item_id`
3. 通过 `item_id` 反查 `items.task_id` 获取上游任务 ID
4. 该 task_id 有下游依赖任务（`task_deps` 表）
5. 下游任务当前状态为 `paused`

**FR-6.2** 实现方式
- 同步函数调用（非 EventBus）：`_trigger_dependent_tasks(container, order)`
- 直接修改 `task.status`：`paused → running`
- 发布事件：`stage='dep_triggered'`（不是 `type='task.started'`）

**FR-6.3** 设计原则
- 仅 `paused` 状态的下游任务会被自动激活
- 避免误启动用户主动 `stopped` 的任务
- `running` 状态本就在执行，无需再触发

### FR-7 接管超时清理

**FR-7.1** TakeoverTimeoutScheduler 配置
- 扫描间隔：`_DEFAULT_SCAN_INTERVAL_MIN = 5`（5 分钟）
- 超时阈值：`TAKEOVER_TIMEOUT_MIN = 30`（30 分钟，对应闲鱼待付款自动关闭）
- 调度器：APScheduler `BackgroundScheduler`（daemon=True）
- 任务 ID：`takeover_timeout_cleanup`，`replace_existing=True`，`max_instances=1`，`coalesce=True`

**FR-7.2** `_run_cleanup_job` 定时任务
- 调用 `container.repo.expire_takeover_pending_orders(timeout_min)`
- 状态流转：`takeover_pending → failed`
- 清空 `confirmed_at`：与 `takeover_cancel` 一致，让订单可被重新接管
- 返回被清理的订单列表，供调用方发布事件/通知下游

**FR-7.3** `expire_takeover_pending_orders` 实现
- 批量 SQL 更新（一次 update，避免 N+1）
- 查询条件：`status == "takeover_pending"` AND `confirmed_at IS NOT NULL` AND `confirmed_at < now - 30min`
- 更新字段：`status="failed"`, `error="接管超时未支付（30 分钟）"`, `confirmed_at=None`

**FR-7.4** 为什么流转到 `failed` 而非 `cancelled`
- 用户未完成支付是异常情况
- `cancelled` 会被误认为是用户主动取消，影响统计准确性
- `failed` 明确表示这是异常终止，便于运营分析

### FR-8 风险等级与分数映射

**FR-8.1** `Evaluator._score_to_risk()` 映射规则
- 使用 `thresholds.auto_buy_score` 和 `thresholds.pass_score`（非硬编码 80）
- 4 级风险：

| 总分范围 | 风险等级 | 后续动作 |
|----------|----------|----------|
| `score >= auto_buy_score`（默认 80，可配置） | `LOW` | 通知 + `auto` 模式下自动抢单 |
| `score >= pass_score`（默认 60） | `MEDIUM` | 仅通知，`confirm`/`semi_auto` 模式下等待用户确认 |
| `score >= 40` | `HIGH` | 不通知（仅记录），用户可在 Dashboard 查看 |
| `score < 40` 或一票否决 | `EXTREME` | 不通知，标记为高风险 |

**FR-8.2** HIGH 级别不通知的原因
- 避免低质量商品打扰用户
- 40-59 分的商品风险较高，主动推送会造成通知噪音
- 用户主动浏览 Dashboard 时仍可查看（有查看价值）

### FR-9 自动抢单完整流程

**FR-9.1** 流程步骤
1. 商品评分 ≥ `auto_buy_score`（默认 80，可配置）
2. 检查任务模式 == `AUTO`（worker._should_buy）
3. 检查风险等级 == `LOW`（eval_result.should_auto_buy）
4. 检查冷却期（worker._in_cooldown）
5. Buyer.buy() 入口
6. 幂等检查：内存 LRU（maxsize=10000）+ DB find_order_by_task_item
7. 节流：`min_interval_between_orders`
8. 频率伪装统计：`record_freq_request(ActionType.LOGIN)`
9. 90 秒总体超时：`asyncio.wait_for(self._do_buy(...), timeout=90.0)`
10. `_do_buy` 子流程：
    - 导航到详情页（domcontentloaded, 20s 超时）
    - 检测已售（`_detect_sold`）→ 已售则抛 `ItemSoldError`
    - 点击"立即购买"（重试 `click_retry_times` 次）
    - 等待订单确认页（`_wait_for_order_page`，含登录页跳转检测）
    - 点击"提交订单/确认购买"（多文案匹配）
    - 提取订单号（`_extract_order_no`）
    - 提取实际价格（`_extract_actual_price`）
    - 价格校验：偏差 > `price_tolerance` (5%) → 抛 `PriceMismatchError`
11. 持久化：`repo.upsert_order(order)` + 发布 `BUY_SUCCEEDED` 事件
12. `stop_on_first_buy`：抢到 1 单后停止本轮
13. `cooldown_after_buy`：30 秒冷却期

### FR-10 BuyerError 异常体系

**FR-10.1** 异常类层次

| 异常类 | 含义 | 触发场景 | HTTP 响应 |
|--------|------|----------|-----------|
| `BuyerError` | 落单流程异常基类 | — | 502 |
| `ButtonNotFoundError` | 找不到「立即购买」按钮 | 页面结构变化 | 502 |
| `ItemSoldError` | 商品已售出 | 已售检测命中 | 409 |
| `OutOfStockError` | 商品已下架/无库存 | 下架检测命中 | 502 |
| `PriceMismatchError` | 价格偏差过大 | 实际价格 vs 预期价格超 5% | 502 |

**FR-10.2** 区分异常类型的原因
- 不同异常对应不同业务语义
- 便于前端给出准确提示（如已售出返回 409 区分业务错误与系统异常）

### FR-11 热更新机制

**FR-11.1** 配置层热更新
- 前端保存 `pass_score` / `auto_buy_score` 后，写盘 + `reload_config()` 重置单例
- 调度流程每次执行都通过 `get_config().eval.pass_score` / `auto_buy_score` 实时获取
- 无需重启服务

**FR-11.2** 运行时层不热更新
- `BuyerConfig` 运行时参数（click_retry_times 等）当前为硬编码
- 修改需重启服务（待 UI 化后会接入热更新）
- `TakeoverTimeoutScheduler` 配置变更也需重启服务

### FR-12 与评估规则的协作

**FR-12.1** 字段共享
- `pass_score` / `auto_buy_score` 存于 `eval` 配置块
- 与"评估规则"页面**共享**，在任一页面修改都会影响另一页面
- 通过 `useConfigStore` 全局状态管理

**FR-12.2** 协作流程
- 评估规则页面配置 `weights`/`thresholds` → 影响评分计算
- 抢单策略页面配置 `pass_score`/`auto_buy_score` → 影响评分阈值判定
- 两页面共享 `eval` 配置块，前端 `useConfigStore` 统一管理

**FR-12.3** 职责分离原则
- 评估规则页面管"怎么算分"
- 抢单策略页面管"分数怎么用"
- 分离后职责清晰，避免配置冗余

---

## 5. 接口定义

所有配置接口统一前缀：`/api/config`，抢单相关接口前缀：`/api/orders`。认证：复用现有 `auth.py` 中间件（**不加入白名单**）。

### 5.1 通用配置接口（9 端点）

#### GET `/api/config`

**描述**：读取完整配置（含 `eval.pass_score` 与 `eval.auto_buy_score`）

**响应**：
```json
{
  "eval": {
    "pass_score": 60,
    "auto_buy_score": 80,
    "weights": {...},
    "thresholds": {...}
  },
  "search": {...},
  "price_strategy": {...}
}
```

#### POST `/api/config/preview`

**描述**：预览改动（dry_run + 校验）

**请求体**：
```json
{
  "payload": {
    "eval": {
      "pass_score": 65,
      "auto_buy_score": 85
    }
  }
}
```

**响应**：
```json
{
  "diffs": [
    {"path": "eval.pass_score", "op": "modify", "old": 60, "new": 65},
    {"path": "eval.auto_buy_score", "op": "modify", "old": 80, "new": 85}
  ],
  "validation": {"ok": true, "errors": []},
  "new_yaml": "..."
}
```

**错误**：
- 422 "pass_score 不能大于 auto_buy_score"（Pydantic `_check_score_order` 校验失败）

#### POST `/api/config/save`

**描述**：保存配置（dry_run + 多版本备份）

**请求体**：
```json
{
  "payload": {"eval": {...}},
  "dry_run": false
}
```

**响应**：
```json
{
  "ok": true,
  "dry_run": false,
  "saved_at": "2026-07-05T10:00:00",
  "diffs": [...],
  "backup": {"filename": "config_20260705_100000.yaml", "pruned": 5},
  "restart_required": false
}
```

#### GET `/api/config/share`

**响应**：`{yaml: "..."}`（脱敏后的 YAML）

#### GET `/api/config/export`

**响应**：文件下载（YAML 文件）

#### POST `/api/config/import`

**请求体**：`{yaml: "..."}`

**响应**：`{ok, saved_at}`

#### GET `/api/config/version`

**响应**：
```json
{
  "items": [
    {"filename": "config_20260705_100000.yaml", "saved_at": "2026-07-05T10:00:00"}
  ]
}
```

#### POST `/api/config/rollback`

**请求体**：`{filename: "config_20260705_100000.yaml"}`

**响应**：`{ok, saved_at}`

#### POST `/api/config/restore-backup`

**请求体**：`{filename: "config_20260705_100000.yaml"}`

**响应**：`{ok}`

### 5.2 抢单相关接口（8 端点）

#### GET `/api/orders`

**Query**：
- `status?`：订单状态过滤
- `task_id?`：按任务过滤
- `item_id?`：按商品过滤
- `limit?=50`：1-200
- `offset?=0`

**响应**：`{items[], count, total, limit, offset}`

#### GET `/api/orders/{order_id}`

**响应**：完整 OrderRow

**错误**：
- 404 "订单不存在"

#### DELETE `/api/orders/{order_id}`

**响应**：`{ok, id}`

#### POST `/api/orders/{order_id}/takeover`

**描述**：发起人工接管

**状态机白名单**：仅 `pending_pay` 可被接管

**响应**：
```json
{"ok": true, "id": "o123", "status": "takeover_pending"}
```

**错误**：
- 400 "仅 pending_pay 可被接管"
- 404 "订单不存在"

#### POST `/api/orders/{order_id}/takeover/confirm`

**描述**：确认支付完成

**响应**：
```json
{"ok": true, "id": "o123", "status": "succeeded"}
```

**副作用**：触发 F-16 `_trigger_dependent_tasks`

#### POST `/api/orders/{order_id}/takeover/cancel`

**描述**：取消接管（回退到 pending_pay，清空 confirmed_at）

**响应**：
```json
{"ok": true, "id": "o123", "status": "pending_pay"}
```

#### PATCH `/api/orders/{order_id}/status`

**描述**：修改订单状态

**请求体**：`{status: "succeeded"|"cancelled"|"failed"}`

**响应**：`{ok, id, status}`

**副作用**：当 status="succeeded" 时触发 F-16

#### POST `/api/orders/manual-takeover`

**描述**：手动抢单

**请求体**：
```json
{
  "task_id": "t1234",
  "item_id": "abc123"
}
```

**响应**：
```json
{
  "ok": true,
  "status": "success",
  "order_id": "o456"
}
```

**错误**：
- 503 "抢单功能未启用：需要以 XH_WITH_SCHEDULER=1 模式启动服务"
- 403 "闲鱼登录已过期"
- 503 "系统正在执行后台浏览器任务，请稍后重试手动抢单"（browser_lock 超时）
- 409 "商品已售出"
- 500 "抢单失败：{error}"

---

## 6. 数据模型

### 6.1 EvalConfig Pydantic 模型（`yaml_config.py`）

抢单策略页面复用 `EvalConfig` 中的 `pass_score` 和 `auto_buy_score` 字段，**不单独定义 BuyerConfig Pydantic 模型**。

| 字段 | 类型 | 必填 | 默认值 | 校验 | 说明 |
|------|------|------|--------|------|------|
| `pass_score` | `int` | 是 | `60` | `ge=0, le=100` | 通过分数（≥此值通知用户） |
| `auto_buy_score` | `int` | 是 | `80` | `ge=0, le=100` | 自动抢单分数（≥此值且任务为 auto 模式才自动拍下） |

**校验器**：`_check_score_order` 强制 `pass_score <= auto_buy_score`，违反时抛 `ValueError`。

### 6.2 BuyerConfig dataclass（`buyer_config.py`，运行时层）

此 dataclass 用于落单流程的可调参数，**当前未 UI 化**，以默认值实例化。`AppConfig` 不含 `BuyerConfig`（运行时 dataclass，未持久化到 YAML）。

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `click_retry_times` | `int` | `2` | 点击"立即购买"按钮的重试次数 |
| `click_retry_interval` | `float` | `1.0` | 两次点击之间的退避秒数 |
| `confirm_button_timeout` | `float` | `10.0` | 等待"提交订单"按钮出现的超时（秒） |
| `price_tolerance` | `float` | `0.05` | 拍下价格相对预期价格的允许偏差（5%） |
| `min_interval_between_orders` | `float` | `0.0` | 两次落单间的最小间隔（秒） |

> **重要澄清**：`BuyerConfig` 中**没有** `daily_limit` / `_daily_buy_count` 等单日抢单上限字段。v1.0 文档声称的"单日抢单上限"在代码中**完全不存在**。防刷单依靠 `stop_on_first_buy` 和 `cooldown_after_buy` 实现。

### 6.3 TaskConfig dataclass（`task.py`，调度层运行时）

`TaskConfig` 是调度层运行时配置（不持久化到 DB），由调度器构造。与抢单策略相关字段：

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `stop_on_first_buy` | `bool` | `True` | 抢到 1 单后立即停止本轮（防刷单） |
| `cooldown_after_buy` | `float` | `30.0` | 拍下后冷却秒数，期间不再下单 |
| `use_cron` | `bool` | `False` | 是否使用 cron 表达式调度 |
| `interval_seconds` | `float` | `60.0` | Worker 循环间隔（秒） |
| `max_items_per_run` | `int` | `20` | 每次循环最多处理多少件商品 |

### 6.4 TaskMode 枚举（4 种，`task.py`）

| 枚举值 | 字符串值 | 说明 |
|--------|----------|------|
| `AUTO` | `auto` | 全自动直接拍 |
| `SEMI_AUTO` | `semi_auto` | 推送确认后拍 |
| `CONFIRM` | `confirm` | 仅推送不拍（默认最保守） |
| `NOTIFY_ONLY` | `notify` | 仅推送 |

### 6.5 OrderStatus 枚举与扩展状态（`order.py`）

`OrderStatus` 枚举仅定义 4 个核心值：

| 枚举值 | 字符串值 | 说明 |
|--------|----------|------|
| `PENDING_PAY` | `pending_pay` | 已拍下，待支付 |
| `PAID` | `paid` | 已支付 |
| `CANCELLED` | `cancelled` | 已取消 |
| `FAILED` | `failed` | 失败（人工处理） |

**`OrderRow.status` 扩展值**（数据库实际存储，但不在 `OrderStatus` 枚举中）：

| 扩展值 | 含义 | 来源 |
|--------|------|------|
| `takeover_pending` | 等待人工接管（中间态） | `POST /takeover` 接口设置 |
| `succeeded` | 抢单成功（与 `paid` 等价，历史遗留） | `takeover_confirm` / `update_status` |

### 6.6 订单状态流转图

```
                ┌──────────────┐
                │ pending_pay  │ ← 自动抢单/手动抢单成功后的初始状态
                └──────┬───────┘
                       │
            ┌──────────┼──────────────────────┐
            │          │                      │
            │          ▼ POST /takeover       │
            │   ┌──────────────────┐          │
            │   │ takeover_pending │          │
            │   └──────┬───────────┘          │
            │          │                      │
            │   ┌──────┴──────────────┐       │
            │   │                     │       │
            │   ▼ POST /confirm       ▼ POST /cancel
            │ ┌─────────────┐   ┌──────────────┐
            │ │ succeeded   │   │ pending_pay  │ (回退，清空 confirmed_at)
            │ └──────┬──────┘   └──────────────┘
            │        │
            │        │ F-16: 触发下游 paused 任务
            │        │
            └────────┼──────────────────────────┐
                     │                          │
                     ▼ PATCH /status            ▼ 接管超时调度器
              ┌─────────────────┐       ┌──────────────┐
              │ cancelled/failed│       │    failed    │ (超时 30 分钟)
              └─────────────────┘       └──────────────┘
```

### 6.7 YAML Schema

```yaml
eval:
  pass_score: 60
  auto_buy_score: 80
```

注：`BuyerConfig` 的运行时参数当前硬编码在 `buyer_config.py`，未持久化到 YAML。`TaskConfig` 同样不持久化，由调度器以默认值构造。

### 6.8 接管超时配置常量

| 常量 | 默认值 | 位置 | 说明 |
|------|--------|------|------|
| `TAKEOVER_TIMEOUT_MIN` | `30` | `api_orders.py` | 接管超时阈值（分钟），对应闲鱼待付款 30 分钟自动关闭 |
| `_DEFAULT_SCAN_INTERVAL_MIN` | `5` | `takeover_timeout_scheduler.py` | 超时扫描间隔（分钟），平衡时效性与 DB 开销 |

### 6.9 BuyerError 异常体系（`order.py`）

| 异常类 | 基类 | HTTP 响应 | 说明 |
|--------|------|-----------|------|
| `BuyerError` | `Exception` | 502 | 落单流程异常基类 |
| `ButtonNotFoundError` | `BuyerError` | 502 | 找不到「立即购买」按钮 |
| `ItemSoldError` | `BuyerError` | 409 | 商品已售出 |
| `OutOfStockError` | `BuyerError` | 502 | 商品已下架/无库存 |
| `PriceMismatchError` | `BuyerError` | 502 | 价格偏差过大（超 5%） |

---

## 7. 性能指标

| 指标 | P50 目标 | P95 目标 | 说明 |
|------|----------|----------|------|
| GET /api/config 读取 | ≤ 50ms | ≤ 150ms | YAML 文件读取 + Pydantic 反序列化 |
| POST /api/config/preview | ≤ 100ms | ≤ 300ms | dry_run + 校验 + diff 计算 |
| POST /api/config/save | ≤ 200ms | ≤ 500ms | 写盘 + reload_config + 多版本备份 |
| GET /api/config/version | ≤ 50ms | ≤ 150ms | 备份目录扫描 |
| POST /api/config/rollback | ≤ 200ms | ≤ 500ms | 备份文件复制 + reload |
| GET /api/orders 列表 | ≤ 100ms | ≤ 300ms | SQL 过滤 + limit/offset |
| POST /api/orders/{id}/takeover | ≤ 50ms | ≤ 150ms | 单条 UPDATE |
| POST /api/orders/{id}/takeover/confirm | ≤ 100ms | ≤ 300ms | UPDATE + F-16 同步触发 |
| POST /api/orders/manual-takeover | 5-30s | 90s | 浏览器自动化全流程（含 90s 超时上限） |
| TakeoverTimeoutScheduler 扫描 | ≤ 100ms | ≤ 500ms | 5 分钟一次的批量 SQL |
| 90 秒总体超时上限 | — | — | `asyncio.wait_for(timeout=90.0)` |
| 接管超时阈值 | — | — | 30 分钟（对应闲鱼待付款自动关闭） |
| 扫描间隔 | — | — | 5 分钟（TakeoverTimeoutScheduler） |
| cooldown_after_buy | — | — | 30 秒（拍下后冷却期） |
| LRU 幂等缓存大小 | — | — | 10000 条 |
| 价格容差 | — | — | 5%（`price_tolerance=0.05`） |

---

## 8. 安全要求

### 8.1 认证与授权

- **SR-8.1.1**：所有 `/api/config/*` 与 `/api/orders/*` 端点必须通过现有 `auth.py` 认证中间件
- **SR-8.1.2**：**不加入认证白名单**（与 `/api/auth/cookie` 等不同）
- **SR-8.1.3**：401 响应必须返回 JSON `{"detail": "Unauthorized"}`（遵循 project_memory 硬约束）
- **SR-8.1.4**：通用配置接口通过 `request.state.user_id` 鉴权

### 8.2 配置校验

- **SR-8.2.1**：Pydantic `_check_score_order` 校验器强制 `pass_score <= auto_buy_score`
- **SR-8.2.2**：前端 `clamp` 函数强制 [0, 100] 范围，处理 `null/NaN/undefined`
- **SR-8.2.3**：保存前必须 dry_run 预览，用户二次确认后才真正写盘
- **SR-8.2.4**：`auto_buy_score` 默认值 80 是**可配置项**，不是硬编码阈值（v1.0 文档错误已修正）

### 8.3 抢单安全护栏

- **SR-8.3.1**：90 秒总体超时（`asyncio.wait_for(timeout=90.0)`）防止浏览器操作无限阻塞
- **SR-8.3.2**：价格容差校验（`price_tolerance=0.05`）防卖家临时涨价
- **SR-8.3.3**：LRU 内存幂等（maxsize=10000）+ DB 幂等兜底，防重复下单
- **SR-8.3.4**：`stop_on_first_buy=True` 抢到 1 单后停止本轮（防刷单）
- **SR-8.3.5**：`cooldown_after_buy=30.0` 拍下后冷却期（防连点）
- **SR-8.3.6**：buyer 注入态校验，`with_browser=False` 模式下拒绝手动抢单
- **SR-8.3.7**：登录态校验，Cookie 失效时拒绝抢单
- **SR-8.3.8**：浏览器锁 `priority="high"` 串行化浏览器操作，超时 10 秒返回 503

### 8.4 接管流程安全

- **SR-8.4.1**：状态机白名单：仅 `pending_pay` 可被接管
- **SR-8.4.2**：拒绝 `takeover_pending → takeover_pending`，避免用户无限点击重置 30 分钟倒计时
- **SR-8.4.3**：拒绝 `succeeded`/`failed`/`cancelled → takeover_pending`，终态订单不可复活
- **SR-8.4.4**：接管超时 30 分钟自动流转到 `failed`（不是 `cancelled`，便于运营分析）
- **SR-8.4.5**：清空 `confirmed_at` 让订单可被重新接管

### 8.5 敏感字段日志规范

- **SR-8.5.1**：敏感字段不日志：仅记录 `user_id / path / op`，不打印 `cookie 明文`/`Token`/`订单号`
- **SR-8.5.2**：配置写盘失败时 `logger.warning()`（遵循 project_memory 硬约束）
- **SR-8.5.3**：抢单异常堆栈仅 `logger.exception`/`logger.debug`，不进结构化日志
- **SR-8.5.4**：F-16 依赖触发仅记录 `upstream_task_id / downstream_id`，不打印订单详情

### 8.6 F-16 依赖触发安全

- **SR-8.6.1**：仅 `paused` 状态的下游任务会被自动激活
- **SR-8.6.2**：避免误启动用户主动 `stopped` 的任务
- **SR-8.6.3**：`running` 状态本就在执行，无需再触发
- **SR-8.6.4**：`_trigger_dependent_tasks` 是同步函数（非 EventBus），直接修改 `task.status`

### 8.7 输入校验

- **SR-8.7.1**：`pass_score` / `auto_buy_score` 必须是 `int`，范围 [0, 100]
- **SR-8.7.2**：`pass_score <= auto_buy_score`（Pydantic 校验器）
- **SR-8.7.3**：`order_id` 必须是合法字符串
- **SR-8.7.4**：`manual-takeover` 请求体必须含 `task_id` 和 `item_id`

### 8.8 多版本备份

- **SR-8.8.1**：每次保存生成备份文件（格式：`config_YYYYMMDD_HHMMSS.yaml`）
- **SR-8.8.2**：超过阈值的旧备份自动清理（pruned）
- **SR-8.8.3**：支持回滚到任意备份版本
- **SR-8.8.4**：支持从备份恢复（不覆盖当前配置）

---

## 9. 前端设计

### 9.1 页面结构

```
frontend/src/pages/Config/
└── BuyerStrategy.tsx          # 抢单策略页面
```

### 9.2 布局规范

```
BuyerStrategy (页面根组件)
├── 顶部操作栏：重置、保存（橙色 #FF6200 主按钮）
├── 分数阈值 Card
│   └── Row (24 栅格)
│       ├── 左 Col (span=12)：auto_buy_score InputNumber
│       └── 右 Col (span=12)：pass_score InputNumber
├── 任务级模式 Card
│   └── Row (gutter=[16,16])
│       ├── notify 模式 Card（蓝色 #1677ff）
│       ├── confirm 模式 Card（黄色 #faad14）
│       └── auto 模式 Card（橙色 #FF6200）
├── 安全护栏 Card
│   ├── 护栏规则列表
│   └── 警告 Alert（"谨慎使用 auto 模式"）
└── Diff 预览 Modal
```

### 9.3 关键交互

| 区域 | 交互行为 |
|------|----------|
| 分数阈值 InputNumber | min=0, max=100, addonAfter="分"；onChange 触发 `update` patch 合并 |
| 数值防御 | `clamp(v)` 函数处理 `null/NaN/undefined` → 0，超出 [0, 100] 截断 |
| 保存按钮 | 主色 `#FF6200`（闲鱼橙）；触发 `previewSave` → 弹出 Diff Modal |
| 重置按钮 | `reset` 函数回滚到原始配置；`hasChanges()` 为 false 时禁用 |
| Diff 预览 Modal | Table 展示 path/op/old/new；操作列显示 Tag（新增=绿/删除=红/修改=橙） |
| 二次确认 | Diff Modal 底部"确认保存"按钮触发 `confirmSave` 真正写盘 |
| 模式卡片配色 | notify=蓝色（安全）、confirm=黄色（注意）、auto=橙色（危险） |

### 9.4 状态管理

```typescript
const { config, load, hasChanges, reset, update, previewSave, confirmSave } = useConfigStore()
```

- `config`：完整配置对象，含 `eval.pass_score` / `eval.auto_buy_score`
- `load()`：进入页面时拉取配置
- `update(patch)`：patch 合并更新
- `hasChanges()`：检测是否有未保存的变更
- `reset()`：回滚到原始配置
- `previewSave()`：dry_run 预览，返回 `DiffChange[]`
- `confirmSave()`：真正写盘

### 9.5 DiffChange 类型

```typescript
type DiffChange = {
  path: string       // 配置路径，如 "eval.pass_score"
  op: string         // 操作：add/delete/modify
  old_value: any     // 原值
  new_value: any     // 新值
}
```

### 9.6 任务级模式说明 Card

- 当前 `MODES` 数组只有 3 个条目（notify/confirm/auto）
- **前端待修正项**：需补充 `semi_auto` 模式卡片
- 每个卡片含：图标 + 标签 + 描述 + 配色
- `semi_auto` 模式需在"任务管理"页面按任务独立配置，本页面仅作说明

### 9.7 安全护栏 Card

- 列表展示实际生效的护栏规则（9 项）
- **前端待修正项**：
  - 第 218 行"auto_buy_score ≥ 80 才启用 auto 模式" → 应改为"auto_buy_score 可配置（默认 80）"
  - 第 220-223 行"单日抢单上限：在 buyer_config.py 中配置（待 UI 化）" → 应改为"防刷单依靠 stop_on_first_buy + cooldown_after_buy"
- 警告 Alert：`type="warning"`, `icon=<WarningOutlined />`，"谨慎使用 auto 模式"

### 9.8 数值防御 clamp 函数

```typescript
const clamp = (v: number | undefined | null): number => {
  if (v == null || isNaN(v)) return 0
  return Math.max(0, Math.min(100, v))
}
```

- 处理后端返回的 `null/NaN/undefined` 异常值
- 防止用户通过浏览器开发者工具输入超范围值
- 应用于 `auto_buy_score` 和 `pass_score` 两个 InputNumber

### 9.9 保存按钮主色

- 使用 `#FF6200`（闲鱼橙）突出抢单操作的重要性
- 与普通配置页面的蓝色主按钮区分
- 提示用户：抢单策略涉及资金安全，操作需谨慎

---

## 10. 部署与集成

### 10.1 后端集成

#### 10.1.1 模块目录

```
src/xianyu_hunter/
├── web/routes/
│   ├── api_config.py                 # 通用配置 9 端点（已存在）
│   └── api_orders.py                 # 抢单相关 8 端点 + F-16 + manual-takeover
├── domain/
│   ├── task.py                       # TaskMode 4 种 + TaskConfig dataclass
│   └── order.py                      # OrderStatus + BuyerError 异常体系
├── modules/
│   ├── buyer_config.py               # BuyerConfig dataclass（运行时层）
│   ├── buyer.py                      # Buyer + 90s 超时 + LRU 幂等
│   ├── worker.py                     # TaskWorker._should_buy/_attempt_buy
│   ├── evaluator.py                  # _score_to_risk 风险映射
│   └── takeover_timeout_scheduler.py # TakeoverTimeoutScheduler
├── infra/
│   ├── yaml_config.py                # EvalConfig Pydantic + _check_score_order
│   ├── repo_orders.py                # expire_takeover_pending_orders
│   └── db_models.py                  # OrderRow
└── web/services/
    └── config_store.py               # 配置状态管理（已存在）
```

#### 10.1.2 路由注册

`api_config.py` 与 `api_orders.py` 已在 `startup.py` 中注册，无需新增。

#### 10.1.3 TakeoverTimeoutScheduler 初始化

服务启动时在 `startup.py` 中初始化：

```python
scheduler = TakeoverTimeoutScheduler(container, timeout_min=30, scan_interval_min=5)
scheduler.start()
```

- `BackgroundScheduler(daemon=True)` 在独立线程运行
- 任务 ID：`takeover_timeout_cleanup`
- `replace_existing=True`：避免重复注册
- `max_instances=1`：避免并发执行
- `coalesce=True`：合并错过的执行

### 10.2 前端集成

#### 10.2.1 路由注册

`BuyerStrategy.tsx` 已在 `App.tsx` 中注册：

```tsx
<Route path="/config/buyer" element={<ConfigLayout><BuyerStrategy /></ConfigLayout>} />
```

#### 10.2.2 菜单注册

`config/menu_registry.yaml`（**已配置**）：

```yaml
- key: buyer
  label: 抢单策略
  icon: ThunderboltOutlined
  path: /config/buyer
  sort_order: 20
  default_visible: true
  category: config
```

### 10.3 配置存储

复用现有 YAML 配置文件：

```yaml
# data/config.yaml
eval:
  pass_score: 60
  auto_buy_score: 80
  weights:
    professional: 30
    credit: 30
    dispute: 25
    price: 15
  thresholds:
    on_sale_count: 30
    # ... 其他评估阈值
```

### 10.4 备份目录

```
data/
├── config.yaml                       # 当前配置
└── backups/
    ├── config_20260705_100000.yaml   # 备份文件（按时间戳命名）
    ├── config_20260704_150000.yaml
    └── ...
```

- 每次保存生成备份
- 超过阈值的旧备份自动清理（pruned）

---

## 11. 验收标准

### 11.1 功能验收

| 编号 | 验收项 | 验收标准 | 测试方法 |
|------|--------|----------|----------|
| AC-1 | 菜单入口 | 侧边栏"配置管理"分组下显示"抢单策略"，点击进入 `/config/buyer` | UI 检查 |
| AC-2 | 配置加载 | `GET /api/config` 返回 `eval.pass_score` 和 `eval.auto_buy_score` | UI 操作 + 后端单测 |
| AC-3 | 修改 pass_score | InputNumber 修改值后 `update` 触发，`hasChanges()` 返回 true | UI 操作 |
| AC-4 | 修改 auto_buy_score | 同 AC-3 | UI 操作 |
| AC-5 | 数值防御 clamp | 输入 null/NaN/undefined/-1/101 时自动截断到 [0, 100] | 前端单测 |
| AC-6 | pass_score > auto_buy_score 校验 | 保存时 Pydantic 校验失败，返回 422 | 后端单测 |
| AC-7 | Diff 预览 | 点击保存弹出 Modal，展示 path/op/old/new | UI 操作 |
| AC-8 | 二次确认 | Diff Modal 底部"确认保存"按钮触发写盘 | UI 操作 |
| AC-9 | 重置功能 | 点击重置回滚到原始配置，`hasChanges()` 返回 false | UI 操作 |
| AC-10 | 热更新 | 保存后无需重启服务，下次调度执行时新配置生效 | 集成测试 |
| AC-11 | 任务级模式说明 | 展示 3 个模式卡片（notify/confirm/auto），含图标+标签+描述 | UI 检查 |
| AC-12 | 安全护栏说明 | 展示 9 项实际护栏规则 + 警告 Alert | UI 检查 |
| AC-13 | 接管流程 | `POST /takeover` 将 pending_pay → takeover_pending | 后端单测 |
| AC-14 | 接管确认 | `POST /takeover/confirm` 将 takeover_pending → succeeded | 后端单测 |
| AC-15 | 接管取消 | `POST /takeover/cancel` 将 takeover_pending → pending_pay（清空 confirmed_at） | 后端单测 |
| AC-16 | 接管状态机白名单 | 仅 pending_pay 可被接管；其他状态返回 400 | 后端单测 |
| AC-17 | 接管超时清理 | 30 分钟超时后 takeover_pending → failed | 集成测试 |
| AC-18 | 接管超时清空 confirmed_at | 超时后 confirmed_at 为 NULL，可重新接管 | 后端单测 |
| AC-19 | F-16 依赖触发 | 订单 succeeded 后下游 paused → running | 集成测试 |
| AC-20 | F-16 仅激活 paused | 下游 running/stopped 状态不被激活 | 集成测试 |
| AC-21 | dep_triggered 事件 | events 表写入 dep_triggered stage 事件 | 后端单测 |
| AC-22 | 手动抢单 | `POST /manual-takeover` 成功返回 order_id | 集成测试 |
| AC-23 | 手动抢单 buyer 注入校验 | with_browser=False 模式下返回 503 | 后端单测 |
| AC-24 | 手动抢单登录态校验 | Cookie 失效时返回 403 | 后端单测 |
| AC-25 | 手动抢单浏览器锁 | priority="high"，超时 10s 返回 503 | 后端单测 |
| AC-26 | ItemSoldError 异常 | 商品已售时返回 409 | 后端单测 |
| AC-27 | 90 秒超时 | 落单流程超过 90s 抛 TimeoutError | 后端单测 |
| AC-28 | 价格容差校验 | 偏差 > 5% 抛 PriceMismatchError | 后端单测 |
| AC-29 | LRU 幂等缓存 | 同一 (task_id, item_id) 第二次调用返回 SKIPPED_DUPLICATE | 后端单测 |
| AC-30 | DB 幂等兜底 | 跨进程重启后仍能识别已下单 | 集成测试 |
| AC-31 | stop_on_first_buy | 抢到 1 单后停止本轮 | 后端单测 |
| AC-32 | cooldown_after_buy | 30 秒冷却期内不执行抢单 | 后端单测 |
| AC-33 | _should_buy 判定 | AUTO=true，NOTIFY_ONLY/CONFIRM/SEMI_AUTO=false | 后端单测 |
| AC-34 | 风险等级映射 | score >= auto_buy_score → LOW；pass_score <= score < auto_buy_score → MEDIUM | 后端单测 |
| AC-35 | 配置备份 | 每次保存生成备份文件 | 集成测试 |
| AC-36 | 配置回滚 | `POST /rollback` 回滚到指定版本 | 集成测试 |
| AC-37 | 配置导入导出 | `POST /import` 导入 YAML；`GET /export` 导出文件 | 集成测试 |
| AC-38 | 配置分享 | `GET /share` 返回脱敏 YAML | 后端单测 |

### 11.2 安全验收

| 编号 | 验收项 | 验收标准 |
|------|--------|----------|
| AC-39 | 认证拦截 | 未认证请求返回 401 JSON `{"detail":"Unauthorized"}` |
| AC-40 | 配置校验 | pass_score > auto_buy_score 返回 422 |
| AC-41 | 接管白名单 | 非 pending_pay 状态返回 400 |
| AC-42 | buyer 注入校验 | with_browser=False 模式下手动抢单返回 503 |
| AC-43 | 登录态校验 | Cookie 失效时抢单返回 403 |
| AC-44 | 敏感字段不日志 | user_id/path/op 出现在日志；cookie 明文/Token/订单号不出现 |
| AC-45 | 配置写盘失败告警 | 写盘失败时 logger.warning() |
| AC-46 | F-16 仅激活 paused | stopped/running 状态不被激活 |

### 11.3 性能验收

| 编号 | 验收项 | 验收标准 |
|------|--------|----------|
| AC-47 | 配置读取 | P95 ≤ 150ms |
| AC-48 | 配置预览 | P95 ≤ 300ms |
| AC-49 | 配置保存 | P95 ≤ 500ms |
| AC-50 | 接管接口 | P95 ≤ 150ms |
| AC-51 | 手动抢单 | 5-30s 完成（含 90s 超时上限） |
| AC-52 | 超时扫描 | P95 ≤ 500ms（5 分钟一次） |

### 11.4 测试用例索引

| 类别 | 测试文件 | 用例数 |
|------|----------|--------|
| 配置读写 | `tests/test_config.py` | 8 |
| Pydantic 校验 | `tests/test_eval_config.py` | 4 |
| 接管流程 | `tests/test_takeover.py` | 6 |
| 接管超时 | `tests/test_takeover_timeout.py` | 4 |
| F-16 依赖触发 | `tests/test_dep_triggered.py` | 4 |
| 手动抢单 | `tests/test_manual_takeover.py` | 5 |
| Buyer 抢单流程 | `tests/test_buyer.py` | 8 |
| LRU 幂等 | `tests/test_buyer_idempotent.py` | 3 |
| 价格容差 | `tests/test_price_tolerance.py` | 3 |
| 前端组件 | `frontend/src/pages/Config/__tests__/` | 6 |
| **合计** | | **51** |

---

## 12. 附录

### 12.1 关键代码位置表

| 模块 | 文件 | 关键行/函数 |
|------|------|-------------|
| 通用配置接口 | `src/xianyu_hunter/web/routes/api_config.py` | `get_config` / `preview_config` / `save_config` / `share_config` / `export_config` / `import_config` / `list_versions` / `rollback_config` / `restore_backup` |
| 抢单订单接口 | `src/xianyu_hunter/web/routes/api_orders.py` | `list_orders` / `get_order` / `delete_order` / `takeover` / `takeover_confirm` / `takeover_cancel` / `update_order_status` / `manual_takeover` / `_trigger_dependent_tasks` |
| 接管超时调度器 | `src/xianyu_hunter/modules/takeover_timeout_scheduler.py` | `TakeoverTimeoutScheduler` / `_run_cleanup_job` |
| 配置模型 | `src/xianyu_hunter/infra/yaml_config.py` | `EvalConfig` / `_check_score_order` |
| 抢单运行时 | `src/xianyu_hunter/modules/buyer_config.py` | `BuyerConfig` dataclass |
| 抢单核心 | `src/xianyu_hunter/modules/buyer.py` | `Buyer` / `buy` / `_do_buy` / `_detect_sold` / `_wait_for_order_page` / `_extract_order_no` / `_extract_actual_price` |
| Worker | `src/xianyu_hunter/modules/worker.py` | `_should_buy` / `_in_cooldown` / `_attempt_buy` |
| 评估器 | `src/xianyu_hunter/modules/evaluator.py` | `_score_to_risk` / `EvaluationThresholds.from_config` |
| 任务领域 | `src/xianyu_hunter/domain/task.py` | `TaskMode` / `TaskConfig` |
| 订单领域 | `src/xianyu_hunter/domain/order.py` | `OrderStatus` / `BuyOutcome` / `BuyResult` / `BuyerError` / `ButtonNotFoundError` / `ItemSoldError` / `OutOfStockError` / `PriceMismatchError` |
| 订单仓储 | `src/xianyu_hunter/infra/repo_orders.py` | `upsert_order` / `get_order` / `list_orders` / `find_order_by_task_item` / `expire_takeover_pending_orders` |
| 前端页面 | `frontend/src/pages/Config/BuyerStrategy.tsx` | `BuyerStrategy` / `MODES` / `clamp` / `updateAutoBuyScore` / `updatePassScore` / `handleSave` / `handleConfirmSave` |
| 配置状态管理 | `frontend/src/stores/configStore.ts` | `useConfigStore` / `DiffChange` |

### 12.2 变更记录

| 版本 | 日期 | 修订人 | 修订内容 |
|------|------|--------|----------|
| v1.0 | 2026-07-05 | 抢单策略 SRS 编写 | 初版（与详细设计 v2.0 对齐），覆盖 12 大功能模块（FR-1~FR-12）、17 个 API 端点（9 通用 + 8 抢单）、9 项数据模型、15 项性能指标、8 类安全要求、9 节前端设计、4 节部署集成、52 条验收标准 |

### 12.3 FAQ / 设计权衡

**Q1：为什么 pass_score 和 auto_buy_score 在"评估规则"页面也能改？**
A：这两个字段存储在 `eval` 配置块下，被"评估规则"和"抢单策略"两个页面共享。这是有意设计，避免配置冗余。在任一页面修改都会同步到另一页面（通过 `useConfigStore` 全局状态管理）。

**Q2：auto 模式下系统会自动付款吗？**
A：不会。auto 模式下系统会自动点击"立即购买"并提交订单，订单状态变为 `pending_pay`。用户需在闲鱼 App 完成支付，或在抢单记录页点击"接管"后通过 `takeover_confirm` 确认支付。

**Q3：为什么 v1.0 文档提到的"单日抢单上限"不存在？**
A：v1.0 文档错误。`BuyerConfig` 中没有 `daily_limit` / `_daily_buy_count` 字段。防刷单依靠 `stop_on_first_buy=True`（抢到 1 单停止本轮）和 `cooldown_after_buy=30.0`（30 秒冷却）实现。

**Q4：v1.0 文档提到的"auto_buy_score ≥ 80 硬阈值"是真的吗？**
A：v1.0 文档错误。`auto_buy_score` 是 `EvalConfig` 的可配置字段（默认 80），不是硬编码阈值。worker.py 通过 `eval_cfg.auto_buy_score if eval_cfg else 80` 读取，用户可在抢单策略页面修改为任意 [0, 100] 值。

**Q5：4 种任务模式可以在运行中切换吗？**
A：模式是任务级配置，在"任务管理"页面按任务独立设置。修改后**下次调度执行时生效**，不影响正在执行的任务。本页面（抢单策略）仅配置分数阈值，不直接设置任务模式。

**Q6：保存配置后需要重启服务吗？**
A：`pass_score` / `auto_buy_score` 不需要。保存接口写盘后调用 `reload_config()` 重置单例，调度流程下次执行时自动获取新配置。`BuyerConfig` 运行时参数（click_retry_times 等）当前为硬编码，修改需重启服务。`TakeoverTimeoutScheduler` 配置变更也需重启服务。

**Q7：接管超时为什么流转到 `failed` 而非 `cancelled`？**
A：用户未完成支付是异常情况，`cancelled` 会被误认为是用户主动取消，影响统计准确性。`failed` 明确表示这是异常终止，便于运营分析。

**Q8：手动抢单和自动抢单有什么区别？**
A：自动抢单要求任务模式为 `AUTO` 且 `with_browser=True`（`XH_WITH_SCHEDULER=1`）；手动抢单由用户在评估明细页点击"手动抢单"按钮触发，调用 `POST /api/orders/manual-takeover`，前置校验 buyer 注入态与登录态后调用 `container.buyer.buy()`。突破纯自动模式限制，让用户保留决策权。

**Q9：F-16 下游依赖触发是什么？**
A：订单状态变为 `succeeded` 时，后端同步调用 `_trigger_dependent_tasks`：通过 `item_id` 反查 `items.task_id`，查询所有依赖此任务的下游任务，将 `paused` 状态的下游任务自动激活为 `running`，并写入 `dep_triggered` stage 事件。仅 `paused` 状态的下游任务会被激活，避免误启动用户主动停止的任务。

**Q10：90 秒总体超时是什么？**
A：`Buyer.buy()` 使用 `asyncio.wait_for(self._do_buy(...), timeout=90.0)` 包裹整个落单流程。超时通常意味着闲鱼页面加载异常或会话失效（RGV587_ERROR），此时抛出 `asyncio.TimeoutError` 并记录"浏览器自动化流程超时（90秒），请检查闲鱼登录状态后重试"。

**Q11：接管超时调度器为什么 5 分钟扫描一次？**
A：平衡时效性与 DB 开销。1 分钟扫描过于频繁（每分钟一次全表扫描），10 分钟扫描会让超时订单多卡 10 分钟才清理。5 分钟是经验值，确保超时订单在 30-35 分钟内被清理。

**Q12：为什么 BuyerConfig 不持久化到 YAML？**
A：`BuyerConfig` 是运行时 dataclass，参数需要根据实际落单情况反复调试。当前硬编码在代码中稳定运行，过早 UI 化会让普通用户困惑。后续版本会接入"高级设置"页面，届时再持久化到 YAML。
