# 批量采集 详细设计文档

## 1. 文档元信息

| 项目     | 内容                                                          |
| -------- | ------------------------------------------------------------- |
| 版本     | v1.0                                                          |
| 日期     | 2026-06-29                                                    |
| 所属菜单 | 系统维护 → 批量采集                                           |
| 关联路由 | 前端 `/batch-refresh`；后端 `/api/batch-refresh/*`            |
| 文档用途 | 开发团队技术指导 + 智能客服系统向量数据库训练素材              |

## 2. 功能概述

**一句话说明**：定时（默认每 30 分钟）刷新在售商品的详情，检测已售状态、补全字段变更，支持暂停/继续/停止与断点续传。

**核心价值**：
- 基于 APScheduler `BackgroundScheduler` 实现定时调度，通过 `run_coroutine_threadsafe` 提交到主事件循环执行 async 采集逻辑（因 `collector.detail()` 依赖 Playwright，绑定主事件循环）；
- 状态机：`idle/running/paused/stopping/unavailable`，通过 `asyncio.Event` 实现暂停/继续，通过 `_stop_flag` 实现停止；
- 任务控制端点立即返回（<500ms），实际暂停/停止在当前 item 处理完成后生效，保证当前 item 的数据库写入完整；
- 进度持久化到 `batch_refresh_progress` 表（`BatchRefreshProgressRow`），暂停/停止时保存未处理 item_id 列表，下次触发从断点续传；
- 变更检测：`_diff_fields()` 对比已有记录与新抓取数据，`_record_change()` 记录变更字段到内存 `_change_log`（上限 1000 条，避免 OOM）；
- 失败重试：单商品失败记录原因并跳过，连续失败超过阈值（默认 3 次）时暂停当前批次；
- 资源调度优化：每个 item 处理后 `await asyncio.sleep(0.3)` 让出事件循环，确保主业务请求优先处理；
- 配置热更新：`PATCH /config` 修改 `interval_minutes` 后立即 `reschedule_job`，`enabled` 仅修改标志位（下次启动按此标志决定）。

## 3. 用户场景

**场景 1：手动触发批量采集**
用户希望立即刷新所有在售商品的详情，进入「批量采集」页面点击「立即触发」按钮。系统返回 `task_id`，状态变为 `running`，前端自动开启高频轮询（3 秒间隔）观察进度。处理过程中可看到当前处理的 item_id、进度百分比、已耗时。

**场景 2：暂停与断点续传**
批量采集进行到一半时，用户希望临时暂停以释放浏览器资源给主业务。点击「暂停」按钮，状态变为 `paused`，实际暂停在当前 item 处理完成后生效（保证写入完整）。系统调用 `_save_progress()` 持久化未处理 item_id 列表。稍后点击「继续」从断点恢复。

**场景 3：停止与未处理项持久化**
用户希望彻底停止当前批次，点击「停止」按钮。状态变为 `stopping`，循环检查 `_stop_flag` 后 break，未处理的 items 持久化到 `batch_refresh_progress` 表（status=`stopped`）。下次触发时 `_load_resumable_progress()` 读取并过滤已售商品后从断点续传。

## 4. 页面布局与交互

### 4.1 组件结构

```
BatchRefresh (默认导出函数)
├── 顶部标题栏：标题 + 「自动刷新」开关 + 「刷新」按钮
├── 状态卡片 Row gutter=16
│   ├── Col：当前状态 Tag（renderStatusTag）
│   ├── Col：已处理/总数 Progress（percent）
│   ├── Col：已耗时 Statistic（formatElapsed）
│   └── Col：上次执行时间 Statistic（formatTime）
├── 控制按钮 Space
│   ├── 「立即触发」（CloudDownloadOutlined，触发后自动开启高频轮询）
│   ├── 「暂停」（PauseCircleOutlined，仅 running 可用）
│   ├── 「继续」（PlayCircleOutlined，仅 paused 可用）
│   └── 「停止」（StopOutlined，running/paused 可用）
├── 配置 Form
│   ├── Switch：启用定时调度
│   ├── InputNumber：间隔分钟（1-1440）
│   └── 「保存配置」按钮（SaveOutlined）
├── 上次结果 Card（last_result）
│   └── Statistic：成功/失败/跳过/是否停止
├── 当前进度 Card（progress）
│   ├── 当前 item_id
│   └── Progress 条
└── 变更日志 Table
    ├── 列：商品 ID / 变更字段（Tag 列表）/ 时间戳
    └── FIELD_LABELS 中文字段名映射
```

### 4.2 关键交互

| 交互                                | 实现位置                            | 说明                                                                |
| ----------------------------------- | ----------------------------------- | ------------------------------------------------------------------- |
| 进入页面加载状态                     | `useEffect(() => loadStatus(), [])` | 调用 `GET /api/batch-refresh/status`                                |
| 自动刷新智能间隔                     | `useEffect([autoRefresh, status?.status])` | running/paused/stopping 时 3s 高频；其他 10s 低频                   |
| 触发后自动开启高频轮询               | `handleTrigger` 内部                | `setAutoRefresh(true)` 立即观察进度                                 |
| 控制按钮禁用逻辑                     | `unavailable` / `running` / `paused` / `stopping` | 仅 `running` 可暂停；仅 `paused` 可继续；`running/paused` 可停止 |
| 静默刷新                             | `loadStatus(true)`                  | 自动轮询时不显示 loading，避免 UI 闪烁                              |
| 配置保存后立即生效                   | `handleSaveConfig`                  | `PATCH /config` 后端 `reschedule_job` 立即重排定时任务              |
| 变更字段中文显示                     | `FIELD_LABELS` 字典                 | `title` → 「标题」，`price` → 「价格」等                            |

## 5. 接口定义

所有接口前缀：`/api/batch-refresh`，路由文件：`src/xianyu_hunter/web/routes/api_batch_refresh.py`。

### 5.1 GET /status

返回调度器当前状态、上次执行结果和最近变更日志。

**响应字段**：

| 字段                | 类型     | 说明                                                                 |
| ------------------- | -------- | -------------------------------------------------------------------- |
| `status`            | string   | `idle` / `running` / `paused` / `stopping` / `unavailable`           |
| `last_run_at`       | string   | 上次执行开始时间 ISO 字符串，无执行时为 `null`                       |
| `last_result`       | object   | `{success, failed, skipped, stopped}`，无结果时为 `null`             |
| `change_log`        | array    | 最近 50 条变更日志（item_id / changed_fields / timestamp）           |
| `enabled`           | bool     | 定时调度是否启用                                                     |
| `interval_minutes`  | int      | 调度间隔（分钟）                                                     |
| `progress`          | object   | 进度信息（task_id/current/total/current_item_id/started_at/elapsed_ms），无批次时为 `null` |
| `control_available` | bool     | 是否允许 pause/resume/stop 操作（`status in (running, paused)`）     |

调度器未启动时返回 `status=unavailable`，其他字段为空值。

### 5.2 POST /trigger

手动触发一次批量采集，返回任务 ID。

**响应**：`{"ok": true, "task_id": 1}`

**错误码**：
- 503：调度器未启动（需 `XH_WITH_SCHEDULER=1` 且 `batch_refresh.enabled=true`）
- 409：已有批次在运行

### 5.3 PATCH /config

运行时热更新配置。

**请求体**（`BatchRefreshConfigPatch`）：

| 字段               | 类型    | 必填 | 范围      | 说明                                              |
| ------------------ | ------- | ---- | --------- | ------------------------------------------------- |
| `interval_minutes` | int     | 否   | 1-1440    | 修改后立即 `reschedule_job`                       |
| `enabled`          | bool    | 否   | -         | 仅修改标志位（不影响已注册的 job，下次启动按此标志决定） |

**响应**：`ok` / `enabled` / `interval_minutes` / `batch_size` / `max_items_per_run`。

### 5.4 POST /pause

暂停当前批次。立即更新状态为 `paused` 并 `clear event`，但实际暂停在当前 item 处理完成后生效（`await event.wait()` 阻塞）。

**响应**：`{"ok": true, "status": "paused"}`

**错误码**：
- 503：调度器未启动
- 409：当前状态不允许暂停（仅 `running` 态可暂停）

### 5.5 POST /resume

继续执行已暂停的批次。`set event` 解除阻塞。

**响应**：`{"ok": true, "status": "running"}`

**错误码**：
- 503：调度器未启动
- 409：当前状态不允许继续（仅 `paused` 态可继续）

### 5.6 POST /stop

停止当前批次。立即更新状态为 `stopping` 并 `set event`（解除暂停阻塞），未处理的 items 持久化以便下次续传。

**响应**：`{"ok": true, "status": "stopping"}`

**错误码**：
- 503：调度器未启动
- 409：当前状态不允许停止（仅 `running/paused` 态可停止）

## 6. 数据结构

### 6.1 BatchRefreshConfigPatch（Pydantic 模型）

```python
class BatchRefreshConfigPatch(BaseModel):
    interval_minutes: int | None = Field(default=None, ge=1, le=1440)
    enabled: bool | None = None
```

### 6.2 batch_refresh_progress 表（BatchRefreshProgressRow）

| 字段                | 类型     | 说明                                                    |
| ------------------- | -------- | ------------------------------------------------------- |
| `id`                | int      | 自增主键                                                |
| `task_id`           | int      | 批次任务 ID（自增，每次 trigger 或定时触发 +1）         |
| `pending_item_ids`  | JSON     | 未处理 item_id 列表（JSON 数组字符串）                  |
| `total`             | int      | 批次总商品数                                            |
| `processed`         | int      | 已处理数量                                              |
| `status`            | string   | `paused` / `stopped` / `running`（进程异常退出残留）    |
| `created_at`        | datetime | 记录创建时间                                            |
| `updated_at`        | datetime | 记录更新时间                                            |

### 6.3 进度信息（progress 字段）

| 字段               | 类型    | 说明                                              |
| ------------------ | ------- | ------------------------------------------------- |
| `task_id`          | int     | 当前批次任务 ID                                   |
| `current`          | int     | 已处理数量                                        |
| `total`            | int     | 总数量                                            |
| `current_item_id`  | string  | 当前正在处理的 item_id，空闲时为 `null`           |
| `started_at`       | string  | 批次开始时间 ISO 字符串                           |
| `elapsed_ms`       | int     | 已耗时毫秒数（前端动态计算，`_get_progress_snapshot` 计算） |

### 6.4 变更日志条目（change_log 数组元素）

| 字段              | 类型     | 说明                              |
| ----------------- | -------- | --------------------------------- |
| `item_id`         | string   | 商品 ID                           |
| `changed_fields`  | string[] | 变更字段名列表（如 `["price", "title"]`） |
| `timestamp`       | string   | 变更检测时间 ISO 字符串           |

## 7. 业务逻辑

### 7.1 状态机

```
                trigger
  idle ─────────────────> running
   ↑                        │
   │                        │ pause
   │                        ↓
   │                     paused
   │                        │
   │           ┌─ resume ───┘
   │           ↓
   │        running
   │                        │
   │                        │ stop
   │                        ↓
   │                     stopping
   │                        │
   │       (current item done)
   │                        │
   └────────────────────────┘
   (all done or stop_flag)

unavailable：调度器未启动（XH_WITH_SCHEDULER≠1 或 batch_refresh.enabled≠true）
```

**状态转换规则**：
- `idle → running`：`trigger_now()` 或定时调度触发
- `running → paused`：`pause()` 设置 `_status="paused"` 并 `clear event`
- `paused → running`：`resume()` 设置 `_status="running"` 并 `set event`
- `running/paused → stopping`：`stop_current()` 设置 `_stop_flag=True` 和 `_status="stopping"` 并 `set event`
- `* → idle`：批次结束（正常完成或 stop_flag 触发 break）

### 7.2 断点续传机制

**暂停时持久化**：
```
pause() 调用
    ↓
_status = "paused"
_pause_event.clear()  # 阻塞 await event.wait()
    ↓
当前 item 处理完成
    ↓
循环进入下一次迭代前 await event.wait() 阻塞
    ↓
（用户 resume 或 stop）
```

注意：暂停本身不写 `_save_progress`，仅在停止或批次正常结束时持久化。如果进程异常退出，残留的 `running` 状态记录会被 `_load_resumable_progress` 视为可续传。

**停止时持久化**：
```
stop_current() 调用
    ↓
_stop_flag = True
_status = "stopping"
_pause_event.set()  # 解除暂停阻塞
    ↓
循环检查 _stop_flag 后 break
    ↓
remaining = items[idx:]  # 含当前 item
_save_progress(task_id, remaining, processed, total, "stopped")
    ↓
finally: _status = "idle"
```

**续传加载**：
```
_run_batch_async 启动时
    ↓
_load_resumable_progress()
    ↓
SELECT * FROM batch_refresh_progress
WHERE status IN ('paused', 'running')
ORDER BY created_at DESC LIMIT 1
    ↓
┌─── 有记录 ───┐    ┌─── 无记录 ───┐
│              │    │              │
│ pending_ids  │    │ 新批次：     │
│ = json.loads │    │ list_unsold_ │
│ (pending_    │    │ items()      │
│  item_ids)   │    │              │
│              │    │ Cookie 同步  │
│ items =      │    │ inject_      │
│  list_items_ │    │ cookie_store │
│  by_ids(...) │    │ _to_worker   │
│              │    │ _browser     │
│ 过滤已售     │    │              │
│ (暂停期间    │    │              │
│  可能已售)   │    │              │
│              │    │              │
│ _clear_      │    │              │
│  progress()  │    │              │
└──────────────┘    └──────────────┘
```

### 7.3 变更检测

`_refresh_one(item_id, existing_item)` 采集单个商品详情并合并写库：

1. 调用 `collector.detail(item_id)` 获取最新详情
2. `_diff_fields(existing_item, new_data)` 对比字段，返回变更字段列表
3. `upsert_item(new_row)` 写入 items 表
4. 若 `is_sold=True`，调用 `mark_sold()` 补写 `sold_detected_at` + 同步 `task_links.display`
5. `sync_item_display_from_detail()` 合并 `task_links.display`
6. `_record_change(item_id, changed_fields)` 记录变更到 `_change_log`

`_change_log` 内存上限 1000 条（`_MAX_CHANGE_LOG`），避免无限增长导致 OOM。`get_status()` 仅返回最近 50 条。

### 7.4 失败重试机制

```
for idx, item in enumerate(items):
    try:
        changed_fields = await self._refresh_one(item_id, item)
        if changed_fields is None:
            skipped += 1
        else:
            success += 1
            if changed_fields:
                self._record_change(item_id, changed_fields)
        consecutive_failures = 0  # 成功时重置
    except Exception as e:
        failed += 1
        consecutive_failures += 1
        if consecutive_failures >= self._fail_pause_threshold:
            # 剩余未处理商品记为 skipped
            processed_count = success + failed + skipped
            remaining_count = len(items) - processed_count
            skipped += remaining_count
            break  # 暂停当前批次
```

`_fail_pause_threshold` 默认 3 次，避免浏览器实例异常时持续无效请求。

### 7.5 资源调度优化

每个 item 处理后 `await asyncio.sleep(_YIELD_INTERVAL)`（0.3 秒）让出事件循环：

```python
# 资源调度优化：让出事件循环给主业务请求
# 不在 finally 中，避免暂停阻塞时也 sleep
await asyncio.sleep(_YIELD_INTERVAL)
```

为什么需要：批量采集是低优先级后台任务，sleep 让出控制权给主业务请求，避免在大量商品采集时阻塞 API 响应。

### 7.6 任务控制立即返回

三个控制端点（pause/resume/stop）均立即返回（<500ms），实际暂停/停止在当前 item 处理完成后生效：

```python
@router.post("/pause")
def pause_batch() -> dict[str, Any]:
    scheduler = _get_scheduler()
    if not scheduler.pause():  # 立即更新状态 + clear event，不等待
        raise HTTPException(status_code=409, ...)
    return {"ok": True, "status": "paused"}  # 立即返回
```

这样保证当前 item 的数据库写入完整，避免数据不一致。

### 7.7 自动刷新轮询策略

前端根据状态动态调整轮询间隔：

```typescript
const interval = status && ['running', 'paused', 'stopping'].includes(status.status)
  ? 3000   // 高频：观察进度
  : 10000  // 低频：减少不必要的网络请求
timerRef.current = setInterval(() => loadStatus(true), interval)
```

`loadStatus(true)` 静默刷新（不显示 loading），避免 UI 闪烁。

## 8. 关键代码位置

| 模块                  | 文件路径                                                            | 行号           |
| --------------------- | ------------------------------------------------------------------- | -------------- |
| 路由声明              | `src/xianyu_hunter/web/routes/api_batch_refresh.py`                 | 18             |
| BatchRefreshConfigPatch | `src/xianyu_hunter/web/routes/api_batch_refresh.py`               | 21-23          |
| _get_scheduler        | `src/xianyu_hunter/web/routes/api_batch_refresh.py`                 | 27-33          |
| GET /status           | `src/xianyu_hunter/web/routes/api_batch_refresh.py`                 | 36-52          |
| POST /trigger         | `src/xianyu_hunter/web/routes/api_batch_refresh.py`                 | 55-73          |
| PATCH /config         | `src/xianyu_hunter/web/routes/api_batch_refresh.py`                 | 76-106         |
| POST /pause           | `src/xianyu_hunter/web/routes/api_batch_refresh.py`                 | 114-132        |
| POST /resume          | `src/xianyu_hunter/web/routes/api_batch_refresh.py`                 | 135-149        |
| POST /stop            | `src/xianyu_hunter/web/routes/api_batch_refresh.py`                 | 152-170        |
| BatchRefreshScheduler 类 | `src/xianyu_hunter/modules/batch_refresh_scheduler.py`           | 52             |
| start/stop            | `src/xianyu_hunter/modules/batch_refresh_scheduler.py`              | 94-120         |
| get_status            | `src/xianyu_hunter/modules/batch_refresh_scheduler.py`              | 126-140        |
| trigger_now           | `src/xianyu_hunter/modules/batch_refresh_scheduler.py`              | 142-156        |
| pause/resume/stop_current | `src/xianyu_hunter/modules/batch_refresh_scheduler.py`          | 158-206        |
| update_config         | `src/xianyu_hunter/modules/batch_refresh_scheduler.py`              | 208-226        |
| _run_batch_job        | `src/xianyu_hunter/modules/batch_refresh_scheduler.py`              | 242-259        |
| _run_batch_async      | `src/xianyu_hunter/modules/batch_refresh_scheduler.py`              | 261-417        |
| _load_resumable_progress | `src/xianyu_hunter/modules/batch_refresh_scheduler.py`           | 419-445        |
| _save_progress        | `src/xianyu_hunter/modules/batch_refresh_scheduler.py`              | 447-482        |
| _clear_progress       | `src/xianyu_hunter/modules/batch_refresh_scheduler.py`              | 484-489        |
| _refresh_one          | `src/xianyu_hunter/modules/batch_refresh_scheduler.py`              | 491+           |
| BatchRefreshProgressRow | `src/xianyu_hunter/infra/db_models.py`                            | -              |
| 前端页面              | `frontend/src/pages/Maintenance/BatchRefresh.tsx`                   | 1-505          |
| 前端 API 客户端       | `frontend/src/api/batchRefresh.ts`                                  | 1-89           |

## 9. 常见问题 FAQ

**Q1：状态显示 `unavailable` 是什么意思？**
A：调度器未启动。需同时满足两个条件：1) 启动 Web 服务时设置环境变量 `XH_WITH_SCHEDULER=1`；2) 配置文件中 `batch_refresh.enabled=true`。检查启动命令是否包含 `--with-scheduler` 参数。

**Q2：触发批量采集返回 409「已有批次在运行」怎么办？**
A：说明上一个批次尚未完成。可通过 `GET /status` 查看当前状态：若为 `running`/`paused`/`stopping`，需先停止当前批次（`POST /stop`）才能触发新批次；若为 `idle`/`unavailable` 可直接触发。

**Q3：暂停后能立即看到状态变化吗？**
A：能。`pause()` 立即更新 `_status="paused"` 并返回，前端 3 秒内会通过轮询看到状态变化。但实际暂停在当前 item 处理完成后生效（`await event.wait()` 阻塞），可能需要几秒到几十秒，取决于当前 item 的采集耗时。

**Q4：停止后未处理的商品会丢失吗？**
A：不会。停止时 `_save_progress()` 将未处理 item_id 列表持久化到 `batch_refresh_progress` 表（status=`stopped`）。下次触发时 `_load_resumable_progress()` 读取并过滤已售商品后从断点续传。

**Q5：变更日志最多保留多少条？**
A：内存上限 1000 条（`_MAX_CHANGE_LOG`），避免无限增长导致 OOM。`GET /status` 仅返回最近 50 条，避免响应体过大。变更日志不持久化，进程重启后清空。

**Q6：连续失败几次会暂停当前批次？**
A：默认 3 次（`_fail_pause_threshold=3`）。连续失败超过阈值时，剩余未处理商品记为 skipped，批次结束。这是为了在浏览器实例异常时避免持续无效请求。

**Q7：每个商品处理间隔多久？**
A：0.3 秒（`_YIELD_INTERVAL`）。这是资源调度优化，让出事件循环给主业务请求，避免批量采集独占事件循环导致 API 响应延迟。

**Q8：单次批次最长执行多久？**
A：1 小时（`_BATCH_TIMEOUT=3600`）。BackgroundScheduler 线程通过 `future.result(timeout=_BATCH_TIMEOUT)` 等待，超时则释放线程避免堆积。单商品采集超时 60 秒（`_DETAIL_TIMEOUT`）。

**Q9：配置 `interval_minutes` 修改后立即生效吗？**
A：是。`PATCH /config` 后端调用 `scheduler.reschedule_job("batch_refresh", trigger="interval", minutes=interval_minutes)` 立即重排定时任务。`enabled` 仅修改标志位，不影响已注册的 job，下次启动时按此标志决定是否注册。

**Q10：批量采集会触发通知吗？**
A：本模块本身不发送通知，但采集到的变更（如已售状态）会通过 `mark_sold()` 触发 `task_links.display` 更新，任务级别的通知由任务调度器在商品状态变化时触发。

**Q11：断点续传时如何处理暂停期间已售的商品？**
A：`_load_resumable_progress()` 读取 pending_item_ids 后，调用 `list_items_by_ids()` 获取完整 item dict，然后过滤掉 `it.get("is_sold")` 为 True 的商品。这样避免重复采集已售商品。

**Q12：为什么任务控制端点能立即返回？**
A：三个端点（pause/resume/stop）只更新内存中的 `_status` 和 `_pause_event`/`_stop_flag`，不等待当前 item 处理完成。实际暂停/停止在循环检查到标志后生效。这样保证 API 响应时间 <500ms，同时保证当前 item 的数据库写入完整。
