# MU2 认证中间件与 Cookie 隔离 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将单用户 Bearer Token 认证改造为多用户 session_token 认证，CookieStore 按 user_id 隔离存储，登录流程接入 UserManager。

**Architecture:** BearerAuthMiddleware 扩展为三路校验（WEB_TOKEN 管理令牌直通 → session_token 查库 → 401），验证通过后注入 `request.state.user_id`。CookieStore 所有方法加 `user_id` 参数，JSON 路径改为 `cookies_{uid}.json`，缓存按 user_id 分桶。登录成功后调用 `identify_or_create → issue_session → export_cookies(user_id)` 完成多用户接入。

**Tech Stack:** Python 3.14 / FastAPI / Starlette / SQLAlchemy / pytest / hmac + secrets + hashlib

---

## 文件结构

| 文件 | 职责 | 操作 |
|------|------|------|
| `backend/xianyu_hunter/web/services/cookie_store.py` | Cookie 按 user_id 隔离存储 | 修改 |
| `backend/xianyu_hunter/web/routes/auth_helpers.py` | 认证响应写入 session_token | 修改 |
| `backend/xianyu_hunter/web/middleware/auth.py` | 三路校验 + user_id 注入 | 修改 |
| `backend/xianyu_hunter/web/routes/unified_login.py` | 登录流程接入 UserManager | 修改 |
| `tests/test_cookie_store_multi_user.py` | CookieStore 多用户隔离测试 | 新建 |
| `tests/test_auth_middleware_mu2.py` | 认证中间件多用户测试 | 新建 |
| `tests/test_unified_login_mu2.py` | 登录流程多用户接入测试 | 新建 |

---

## Task 1: CookieStore 扩展 user_id 维度

**Files:**
- Modify: `backend/xianyu_hunter/web/services/cookie_store.py`
- Test: `tests/test_cookie_store_multi_user.py`

### Step 1: 编写多用户 Cookie 隔离测试

- [ ] **1.1 编写失败测试**

```python
# tests/test_cookie_store_multi_user.py
"""CookieStore 多用户隔离测试"""
import json
import os
import tempfile
from pathlib import Path
from unittest.mock import patch

import pytest

from xianyu_hunter.web.services.cookie_store import CookieStore


@pytest.fixture
def store(tmp_path, monkeypatch):
    """创建使用临时目录的 CookieStore 实例"""
    monkeypatch.setattr("xianyu_hunter.web.services.cookie_store.Path", lambda *args: tmp_path / args[-1] if args else tmp_path)
    s = CookieStore()
    # 重置缓存
    s._cache = {}
    s._cache_ts = 0.0
    return s


def test_export_cookies_writes_user_specific_file(store, tmp_path):
    """export_cookies 按 user_id 写入独立文件"""
    cookies = [{"name": "unb", "value": "12345678", "domain": ".goofish.com"}]
    store.export_cookies(cookies, method="browser", user_id="user_a")
    store.export_cookies(cookies, method="browser", user_id="user_b")

    # 两个用户各有一个独立的 JSON 文件
    assert (tmp_path / "cookies_user_a.json").exists()
    assert (tmp_path / "cookies_user_b.json").exists()


def test_has_valid_cookies_isolated_per_user(store, tmp_path):
    """不同用户的 Cookie 互不影响"""
    cookies_a = [{"name": "unb", "value": "12345678", "domain": ".goofish.com"}]
    store.export_cookies(cookies_a, method="browser", user_id="user_a")

    # user_a 有 Cookie
    assert store.has_valid_cookies(user_id="user_a") is True
    # user_b 没有 Cookie
    assert store.has_valid_cookies(user_id="user_b") is False


def test_cache_is_per_user(store, tmp_path):
    """缓存按 user_id 分桶，互不污染"""
    cookies_a = [{"name": "unb", "value": "12345678", "domain": ".goofish.com"}]
    store.export_cookies(cookies_a, method="browser", user_id="user_a")

    # user_a 读取会缓存
    store.has_valid_cookies(user_id="user_a")
    assert "user_a" in store._cache

    # user_b 缓存应为空
    assert "user_b" not in store._cache


def test_default_user_id_backward_compat(store, tmp_path):
    """不传 user_id 时使用 default，向后兼容"""
    cookies = [{"name": "unb", "value": "12345678", "domain": ".goofish.com"}]
    store.export_cookies(cookies, method="browser")

    assert (tmp_path / "cookies_default.json").exists()
    assert store.has_valid_cookies() is True
```

- [ ] **1.2 运行测试确认失败**

Run: `python -m pytest tests/test_cookie_store_multi_user.py -v --tb=short`
Expected: FAIL — `export_cookies() got an unexpected keyword argument 'user_id'`

- [ ] **1.3 实现 CookieStore user_id 维度**

修改 `backend/xianyu_hunter/web/services/cookie_store.py`：

```python
# 替换模块级常量（约 line 29）
# 旧: _COOKIE_JSON_FILE = Path("data") / "cookies.json"
# 新:
_COOKIE_JSON_DIR = Path("data")

def _cookie_json_path(user_id: str = "default") -> Path:
    """按 user_id 生成 Cookie JSON 文件路径"""
    return _COOKIE_JSON_DIR / f"cookies_{user_id}.json"
```

```python
# 修改 __init__（约 line 104-110）
def __init__(self) -> None:
    self._lock = threading.RLock()
    # 缓存按 user_id 分桶：{user_id: (data, timestamp)}
    self._cache: dict[str, tuple[dict | None, float]] = {}
    _COOKIE_JSON_DIR.mkdir(parents=True, exist_ok=True)
```

```python
# 修改所有公共方法签名，加 user_id="default" 参数
def has_valid_cookies(self, user_id: str = "default") -> bool:
    data = self._read_json(user_id)
    # ... 其余逻辑不变

def validate_cookies_with_expiry(self, user_id: str = "default") -> tuple[bool, str]:
    data = self._read_json(user_id)
    # ... 其余逻辑不变

def get_cookie_info(self, user_id: str = "default") -> dict:
    data = self._read_json(user_id)
    # ... 其余逻辑不变

def get_cookie_expiry(self, user_id: str = "default") -> float | None:
    data = self._read_json(user_id)
    # ... 其余逻辑不变

def export_cookies(self, cookies: list[dict], method: str = "unknown", user_id: str = "default") -> bool:
    # ... 过滤逻辑不变
    success = self._write_json(data, user_id)
    # ... 同步 SQLite 逻辑不变

def update_cookie_values(self, updates: dict[str, str], user_id: str = "default") -> bool:
    # ... 持锁逻辑不变，_read_json/_write_json 传 user_id

def upsert_cookie_values(self, upserts: dict[str, dict], user_id: str = "default") -> bool:
    # ... 持锁逻辑不变，_read_json/_write_json 传 user_id
```

```python
# 修改 invalidate_cache（约 line 350）
def invalidate_cache(self, user_id: str | None = None) -> None:
    """清除缓存。传 user_id 清除指定用户，不传清除全部"""
    with self._lock:
        if user_id:
            self._cache.pop(user_id, None)
        else:
            self._cache.clear()
```

```python
# 修改 _read_json（约 line 362）
def _read_json(self, user_id: str = "default") -> dict | None:
    """读取指定用户的 JSON Cookie 文件（带缓存）"""
    with self._lock:
        cached = self._cache.get(user_id)
        if cached and (time.time() - cached[1]) < _CACHE_TTL:
            return cached[0]
    path = _cookie_json_path(user_id)
    if not path.exists():
        return None
    try:
        data = json.loads(path.read_text(encoding="utf-8"))
        with self._lock:
            self._cache[user_id] = (data, time.time())
        return data
    except (json.JSONDecodeError, OSError):
        return None
```

```python
# 修改 _write_json（约 line 378）
def _write_json(self, data: dict, user_id: str = "default") -> bool:
    """原子写入指定用户的 JSON Cookie 文件"""
    path = _cookie_json_path(user_id)
    try:
        tmp = path.with_suffix(".tmp")
        tmp.parent.mkdir(parents=True, exist_ok=True)
        tmp.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")
        tmp.replace(path)
        with self._lock:
            self._cache[user_id] = (data, time.time())
        logger.info("Cookie 已导出到 JSON [%s]: %d 个, method=%s", user_id, data.get("cookie_count", 0), data.get("method"))
        return True
    except OSError as e:
        logger.error("写入 Cookie JSON 失败 [%s]: %s", user_id, e)
        return False
```

- [ ] **1.4 运行测试确认通过**

Run: `python -m pytest tests/test_cookie_store_multi_user.py -v --tb=short`
Expected: PASS — 4 tests passed

- [ ] **1.5 运行现有 cookie_store 测试确认无回归**

Run: `python -m pytest tests/test_cookie_layer_sync_fix.py tests/test_cookie_layers_sync_repro.py -v --tb=short -x`
Expected: 无新增失败（预存失败可接受）

- [ ] **1.6 提交**

```bash
git add tests/test_cookie_store_multi_user.py backend/xianyu_hunter/web/services/cookie_store.py
git commit -m "feat(mu2): CookieStore 扩展 user_id 维度，按用户隔离 JSON 文件与缓存"
```

---

## Task 2: auth_helpers.py 支持 session_token

**Files:**
- Modify: `backend/xianyu_hunter/web/routes/auth_helpers.py`
- Test: `tests/test_auth_middleware_mu2.py`

### Step 2: 编写 session_token 响应测试

- [ ] **2.1 编写失败测试**

```python
# tests/test_auth_middleware_mu2.py
"""MU2 认证中间件与 auth_helpers 多用户测试"""
import pytest
from xianyu_hunter.web.routes.auth_helpers import make_auth_response


def test_make_auth_response_with_session_token():
    """传入 session_token 时写入 session_token 而非 web_token"""
    resp = make_auth_response({"ok": True}, session_token="abc123session")
    assert "xh_token" in resp.headers.get("set-cookie", "")
    # xh_token 的值应该是 session_token，可通过 resp.cookies 验证
    assert resp.cookies.get("xh_token") == "abc123session"


def test_make_auth_response_without_session_token_fallback():
    """不传 session_token 时回退到 web_token（向后兼容）"""
    resp = make_auth_response({"ok": True})
    # 仍然写入 web_token
    from xianyu_hunter.config import get_settings
    assert resp.cookies.get("xh_token") == get_settings().web_token
```

- [ ] **2.2 运行测试确认失败**

Run: `python -m pytest tests/test_auth_middleware_mu2.py::test_make_auth_response_with_session_token -v`
Expected: FAIL — `make_auth_response() got an unexpected keyword argument 'session_token'`

- [ ] **2.3 实现 session_token 支持**

修改 `backend/xianyu_hunter/web/routes/auth_helpers.py`：

```python
"""认证辅助函数 - 被多个 auth 子模块共享的工具函数"""
from __future__ import annotations

from fastapi.responses import JSONResponse

from xianyu_hunter.config import get_settings


def make_auth_response(data: dict, session_token: str | None = None) -> JSONResponse:
    """构建带 xh_token 认证 Cookie 的响应

    Cookie 注入成功后，用户需要同时获得 Web 会话认证，
    否则后续 /api/tasks 等 API 调用都会 401。

    Args:
        data: 响应 JSON 数据
        session_token: 多用户会话令牌。传入时写入 session_token；
                       不传时回退到全局 web_token（向后兼容单用户模式）
    """
    resp = JSONResponse(content=data)
    token = session_token if session_token else get_settings().web_token
    resp.set_cookie(
        key="xh_token",
        value=token,
        httponly=True,
        samesite="lax",
        max_age=86400 * 30,  # 30 天
    )
    return resp
```

- [ ] **2.4 运行测试确认通过**

Run: `python -m pytest tests/test_auth_middleware_mu2.py -v --tb=short`
Expected: PASS — 2 tests passed

- [ ] **2.5 提交**

```bash
git add tests/test_auth_middleware_mu2.py backend/xianyu_hunter/web/routes/auth_helpers.py
git commit -m "feat(mu2): auth_helpers 支持 session_token 参数，向后兼容 web_token"
```

---

## Task 3: auth.py 认证中间件三路校验改造

**Files:**
- Modify: `backend/xianyu_hunter/web/middleware/auth.py`
- Test: `tests/test_auth_middleware_mu2.py`

### Step 3: 编写三路校验测试

- [ ] **3.1 编写失败测试**

追加到 `tests/test_auth_middleware_mu2.py`：

```python
import asyncio
from unittest.mock import MagicMock, patch
from starlette.testclient import TestClient
from fastapi import FastAPI


def _build_app_with_middleware():
    """构建带 BearerAuthMiddleware 的测试 app"""
    app = FastAPI()
    from xianyu_hunter.web.middleware.auth import setup_auth_middleware
    setup_auth_middleware(app)

    @app.get("/api/whoami")
    def whoami(request):
        # 返回 request.state.user_id（由中间件注入）
        uid = getattr(request.state, "user_id", "not_set")
        return {"user_id": uid}

    return app


def test_web_token_passes_through():
    """WEB_TOKEN 管理令牌直通，user_id 设为 default"""
    from xianyu_hunter.config import get_settings
    app = _build_app_with_middleware()
    client = TestClient(app)
    token = get_settings().web_token
    resp = client.get("/api/whoami", headers={"Authorization": f"Bearer {token}"})
    assert resp.status_code == 200
    assert resp.json()["user_id"] == "default"


def test_session_token_injects_user_id():
    """有效 session_token 注入对应 user_id"""
    app = _build_app_with_middleware()
    client = TestClient(app)

    with patch("xianyu_hunter.web.services.user_manager.get_user_manager") as mock:
        mgr = MagicMock()
        mgr.verify_session.return_value = "user_abc"
        mock.return_value = mgr

        resp = client.get("/api/whoami", headers={"Authorization": "Bearer valid_session_token"})
        assert resp.status_code == 200
        assert resp.json()["user_id"] == "user_abc"
        mgr.verify_session.assert_called_once_with("valid_session_token")


def test_invalid_token_returns_401():
    """无效 token 返回 401"""
    app = _build_app_with_middleware()
    client = TestClient(app)

    with patch("xianyu_hunter.web.services.user_manager.get_user_manager") as mock:
        mgr = MagicMock()
        mgr.verify_session.return_value = None
        mock.return_value = mgr

        resp = client.get("/api/whoami", headers={"Authorization": "Bearer invalid"})
        assert resp.status_code == 401
        assert resp.json()["detail"] == "Unauthorized"


def test_public_path_no_auth_required():
    """公开路径不需要认证"""
    app = _build_app_with_middleware()
    client = TestClient(app)
    resp = client.get("/healthz")
    # /healthz 不在路由中，但中间件放行后会 404（而非 401）
    assert resp.status_code != 401
```

- [ ] **3.2 运行测试确认失败**

Run: `python -m pytest tests/test_auth_middleware_mu2.py::test_session_token_injects_user_id -v`
Expected: FAIL — `user_id` 为 "not_set"（中间件未注入）

- [ ] **3.3 实现三路校验**

修改 `backend/xianyu_hunter/web/middleware/auth.py` 的 `dispatch` 方法：

```python
async def dispatch(self, request: Request, call_next):
    # 公开路径直接放行
    for prefix in PUBLIC_PREFIXES:
        if request.url.path.startswith(prefix):
            return await call_next(request)

    # 根路径放行（前端会处理认证跳转）
    if request.url.path == "/":
        return await call_next(request)

    from xianyu_hunter.config import get_settings
    from loguru import logger

    # 从 Authorization header 或 cookie 中取 token
    auth_header = request.headers.get("authorization", "")
    if auth_header.startswith("Bearer "):
        req_token = auth_header[7:]
    else:
        req_token = request.cookies.get("xh_token", "")

    if not req_token:
        if request.url.path.startswith("/api/"):
            return JSONResponse(status_code=401, content={"detail": "Unauthorized"})
        return await call_next(request)

    web_token = get_settings().web_token

    # 路径 1：WEB_TOKEN 管理令牌直通（向后兼容单用户模式）
    if hmac.compare_digest(req_token.encode(), web_token.encode()):
        request.state.user_id = "default"
        return await call_next(request)

    # 路径 2：session_token 多用户会话校验
    try:
        from xianyu_hunter.web.services.user_manager import get_user_manager
        user_id = get_user_manager().verify_session(req_token)
    except Exception as e:
        logger.debug("[Auth] session 校验异常: %s", e)
        user_id = None

    if user_id:
        request.state.user_id = user_id
        return await call_next(request)

    # 路径 3：校验失败
    logger.debug(
        f"[Auth] path={request.url.path} "
        f"has_cookie={'xh_token' in request.cookies} "
        f"web_token_match=False session_invalid=True"
    )
    if request.url.path.startswith("/api/"):
        return JSONResponse(status_code=401, content={"detail": "Unauthorized"})
    return await call_next(request)
```

- [ ] **3.4 运行测试确认通过**

Run: `python -m pytest tests/test_auth_middleware_mu2.py -v --tb=short`
Expected: PASS — 6 tests passed（含 Task 2 的 2 个）

- [ ] **3.5 运行现有认证相关测试确认无回归**

Run: `python -m pytest tests/test_login_cookie_injection.py -v --tb=short -x`
Expected: 无新增失败

- [ ] **3.6 提交**

```bash
git add tests/test_auth_middleware_mu2.py backend/xianyu_hunter/web/middleware/auth.py
git commit -m "feat(mu2): 认证中间件三路校验(WEB_TOKEN直通+session_token+401)，注入 user_id"
```

---

## Task 4: unified_login.py 登录流程接入 UserManager

**Files:**
- Modify: `backend/xianyu_hunter/web/routes/unified_login.py`
- Test: `tests/test_unified_login_mu2.py`

### Step 4: 编写登录流程多用户接入测试

- [ ] **4.1 编写失败测试**

```python
# tests/test_unified_login_mu2.py
"""MU2 登录流程多用户接入测试"""
import pytest
from unittest.mock import MagicMock, patch, AsyncMock
from xianyu_hunter.web.routes.unified_login import _session, _reset_session


@pytest.fixture(autouse=True)
def reset_session():
    """每个测试前重置登录会话"""
    _reset_session()
    yield
    _reset_session()


def test_login_success_calls_identify_and_issue_session():
    """登录成功后调用 identify_or_create + issue_session + export_cookies(user_id)"""
    fake_cookies = [
        {"name": "unb", "value": "12345678", "domain": ".goofish.com"},
        {"name": "cookie2", "value": "a" * 32, "domain": ".taobao.com"},
    ]

    with patch("xianyu_hunter.web.routes.unified_login.get_cookie_store") as mock_store, \
         patch("xianyu_hunter.web.routes.unified_login.get_user_manager") as mock_mgr, \
         patch("xianyu_hunter.web.routes.unified_login._verify_cookies", return_value=True), \
         patch("xianyu_hunter.web.routes.unified_login._trigger_userinfo_refresh"), \
         patch("xianyu_hunter.web.routes.unified_login._trigger_session_start"):

        store = MagicMock()
        store._read_json.return_value = {"cookies": fake_cookies}
        mock_store.return_value = store

        mgr = MagicMock()
        mgr.identify_or_create.return_value = "12345678"
        mgr.issue_session.return_value = "session_token_abc"
        mock_mgr.return_value = mgr

        # 模拟后台线程中浏览器登录成功
        from xianyu_hunter.web.routes.unified_login import _background_wait
        import subprocess
        proc = MagicMock(spec=subprocess.Popen)
        proc.poll.return_value = None
        proc.wait.return_value = None
        proc.communicate.return_value = (b"", b"")

        import tempfile, json
        from pathlib import Path
        status_file = Path(tempfile.gettempdir()) / "test_mu2_status.json"
        status_file.write_text(json.dumps({"status": "success", "cookie_count": 2}), encoding="utf-8")

        _background_wait(proc, status_file, "browser")

        # 验证 identify_or_create 被调用
        mgr.identify_or_create.assert_called_once()
        # 验证 issue_session 被调用
        mgr.issue_session.assert_called_once_with("12345678")
        # 验证 export_cookies 按 user_id 存储
        store.export_cookies.assert_called_once()
        call_kwargs = store.export_cookies.call_args
        assert call_kwargs[1].get("user_id") == "12345678" or call_kwargs[0][-1] == "12345678"


def test_login_status_returns_session_token_in_cookie():
    """登录成功状态响应中 xh_token cookie 为 session_token"""
    with patch("xianyu_hunter.web.routes.unified_login.get_user_manager") as mock_mgr, \
         patch("xianyu_hunter.web.routes.unified_login.get_cookie_store") as mock_store, \
         patch("xianyu_hunter.web.routes.unified_login._verify_cookies", return_value=True), \
         patch("xianyu_hunter.web.routes.unified_login._trigger_userinfo_refresh"), \
         patch("xianyu_hunter.web.routes.unified_login._trigger_session_start"), \
         patch("xianyu_hunter.web.routes.unified_login._ensure_session_cookies_injected", new_callable=AsyncMock):

        store = MagicMock()
        store._read_json.return_value = {"cookies": [{"name": "unb", "value": "12345678"}]}
        mock_store.return_value = store

        mgr = MagicMock()
        mgr.identify_or_create.return_value = "12345678"
        mgr.issue_session.return_value = "session_token_xyz"
        mock_mgr.return_value = mgr

        # 模拟成功状态
        _session["status"] = "success"
        _session["method"] = "browser"

        from xianyu_hunter.web.routes.unified_login import login_status
        import asyncio
        result = asyncio.run(login_status())

        # make_auth_response 返回 JSONResponse，检查 cookie
        assert hasattr(result, "cookies")
        assert result.cookies.get("xh_token") == "session_token_xyz"
```

- [ ] **4.2 运行测试确认失败**

Run: `python -m pytest tests/test_unified_login_mu2.py -v --tb=short`
Expected: FAIL — `export_cookies` 未传 `user_id` / cookie 中无 session_token

- [ ] **4.3 实现登录流程接入**

修改 `backend/xianyu_hunter/web/routes/unified_login.py`：

1. 新增 `_finalize_multi_user_login` 函数（在 `_trigger_session_start` 之后）：

```python
def _finalize_multi_user_login() -> str | None:
    """登录成功后完成多用户接入：识别用户 + 签发会话 + 按用户存储 Cookie

    Returns:
        session_token 或 None（失败时降级为单用户模式）
    """
    try:
        from xianyu_hunter.web.services.user_manager import get_user_manager
        store = get_cookie_store()
        store.invalidate_cache()
        data = store._read_json("default")  # 登录子进程写入 default 文件
        if not data or not data.get("cookies"):
            logger.warning("多用户接入失败：无 Cookie 数据")
            return None

        cookies = data["cookies"]
        mgr = get_user_manager()
        user_id = mgr.identify_or_create(cookies)
        session_token = mgr.issue_session(user_id)

        # 将 Cookie 从 default 迁移到 user_id 维度
        store.export_cookies(cookies, method="login", user_id=user_id)
        logger.info("多用户登录完成: user_id=%s", user_id)
        return session_token
    except Exception as e:
        logger.error("多用户接入异常，降级为单用户模式: %s", e)
        return None
```

2. 修改 `_background_wait`（约 line 512-517），在 success 分支增加多用户接入：

```python
    if file_status == "success":
        _trigger_userinfo_refresh()
        _trigger_session_start()
        # MU2: 多用户接入
        _finalize_multi_user_login()
        logger.info("浏览器登录成功 (cookie_count=%s)", data.get("cookie_count", "?"))
```

3. 修改 `_background_wait_qr`（约 line 451-455），在 success 分支增加多用户接入：

```python
    if final_state == "success":
        _session["status"] = "success"
        _session["message"] = "扫码登录成功"
        _trigger_userinfo_refresh()
        _trigger_session_start()
        # MU2: 多用户接入
        _finalize_multi_user_login()
```

4. 修改 `login_status` 端点（约 line 588-597），在 success 时使用 session_token：

```python
    if should_start_hooks:
        _trigger_userinfo_refresh()
        _trigger_session_start()
        # MU2: 多用户接入
        _finalize_multi_user_login()

    if result["status"] == "success":
        await _ensure_session_cookies_injected()

    # 登录成功时设置 xh_token cookie
    if result["status"] == "success":
        # MU2: 优先使用 session_token
        session_token = _session.get("session_token")
        return make_auth_response(result, session_token=session_token)

    return result
```

5. 在 `_finalize_multi_user_login` 中将 session_token 存入 `_session`：

```python
def _finalize_multi_user_login() -> str | None:
    # ...（前面的逻辑）
        session_token = mgr.issue_session(user_id)
        with _session_lock:
            _session["session_token"] = session_token
            _session["current_user_id"] = user_id
        # ...（后续逻辑）
```

6. 在 `_reset_session` 中增加新字段：

```python
def _reset_session() -> None:
    global _session
    with _session_lock:
        _kill_proc(_session.get("proc"))
        _session = {
            "method": None,
            "status": "idle",
            "message": "",
            "status_file": None,
            "proc": None,
            "pid": None,
            "qr_png_b64": None,
            "started_at": 0.0,
            "cookies_injected": False,
            "session_token": None,      # MU2: 多用户会话令牌
            "current_user_id": None,    # MU2: 当前登录用户 ID
        }
```

- [ ] **4.4 运行测试确认通过**

Run: `python -m pytest tests/test_unified_login_mu2.py -v --tb=short`
Expected: PASS — 2 tests passed

- [ ] **4.5 提交**

```bash
git add tests/test_unified_login_mu2.py backend/xianyu_hunter/web/routes/unified_login.py
git commit -m "feat(mu2): 登录流程接入 UserManager(identify_or_create+issue_session+export_cookies)"
```

---

## Task 5: cookie_inject.py 与 auth_query.py 向后兼容

**Files:**
- Modify: `backend/xianyu_hunter/web/routes/cookie_inject.py`
- Modify: `backend/xianyu_hunter/web/routes/auth_query.py`
- Test: `tests/test_auth_middleware_mu2.py`

### Step 5: 适配 Cookie 注入与状态查询

- [ ] **5.1 检查 cookie_inject.py 的 export_cookies 调用**

Run: `grep -n "export_cookies\|has_valid_cookies\|get_cookie_info\|validate_cookies_with_expiry" backend/xianyu_hunter/web/routes/cookie_inject.py backend/xianyu_hunter/web/routes/auth_query.py backend/xianyu_hunter/web/routes/api_auth.py`
记录所有调用点。

- [ ] **5.2 修改 cookie_inject.py 的调用加 user_id="default"**

对所有 `get_cookie_store().export_cookies(...)` / `has_valid_cookies()` 调用，补充 `user_id="default"` 参数（保持单用户场景向后兼容，MU3 再实现切换）。

- [ ] **5.3 修改 auth_query.py 的调用加 user_id="default"**

对 `get_cookie_info()` / `has_valid_cookies()` / `validate_cookies_with_expiry()` 调用，补充 `user_id="default"`。

- [ ] **5.4 全局搜索遗漏调用点**

Run: `grep -rn "get_cookie_store\(\)\." backend/xianyu_hunter/ --include="*.py" | grep -v "test\|__pycache__"`
逐一检查并补充 `user_id` 参数。

- [ ] **5.5 运行全量认证测试**

Run: `python -m pytest tests/test_auth_middleware_mu2.py tests/test_unified_login_mu2.py tests/test_cookie_store_multi_user.py tests/test_login_cookie_injection.py tests/test_api_anticrawl.py -v --tb=short -x`
Expected: 全部新测试通过，无新增回归

- [ ] **5.6 提交**

```bash
git add backend/xianyu_hunter/web/routes/cookie_inject.py backend/xianyu_hunter/web/routes/auth_query.py
git commit -m "fix(mu2): cookie_inject/auth_query 调用补充 user_id=default 向后兼容"
```

---

## Task 6: 集成测试与回归验证

**Files:**
- Test: `tests/test_mu2_integration.py`

### Step 6: 端到端多用户隔离测试

- [ ] **6.1 编写集成测试**

```python
# tests/test_mu2_integration.py
"""MU2 端到端集成测试：认证中间件 + CookieStore + UserManager 协作"""
import pytest
from unittest.mock import MagicMock, patch


def test_full_flow_web_token_compat():
    """WEB_TOKEN 用户访问 API 时 user_id=default，CookieStore 读写 default 文件"""
    from starlette.testclient import TestClient
    from fastapi import FastAPI
    from xianyu_hunter.web.middleware.auth import setup_auth_middleware
    from xianyu_hunter.config import get_settings

    app = FastAPI()
    setup_auth_middleware(app)

    @app.get("/api/test")
    def test_endpoint(request):
        return {"user_id": getattr(request.state, "user_id", "not_set")}

    client = TestClient(app)
    resp = client.get("/api/test", headers={"Authorization": f"Bearer {get_settings().web_token}"})
    assert resp.status_code == 200
    assert resp.json()["user_id"] == "default"


def test_session_token_and_web_token_coexist():
    """session_token 用户和 WEB_TOKEN 管理员可以同时访问"""
    from starlette.testclient import TestClient
    from fastapi import FastAPI
    from xianyu_hunter.web.middleware.auth import setup_auth_middleware
    from xianyu_hunter.config import get_settings

    app = FastAPI()
    setup_auth_middleware(app)

    @app.get("/api/me")
    def me(request):
        return {"user_id": getattr(request.state, "user_id", "not_set")}

    client = TestClient(app)

    # WEB_TOKEN 用户
    resp1 = client.get("/api/me", headers={"Authorization": f"Bearer {get_settings().web_token}"})
    assert resp1.json()["user_id"] == "default"

    # session_token 用户
    with patch("xianyu_hunter.web.services.user_manager.get_user_manager") as mock:
        mgr = MagicMock()
        mgr.verify_session.return_value = "user_xyz"
        mock.return_value = mgr
        resp2 = client.get("/api/me", headers={"Authorization": "Bearer session_abc"})
        assert resp2.json()["user_id"] == "user_xyz"


def test_cookie_store_isolation_no_cross_contamination():
    """两个用户的 Cookie 文件完全隔离"""
    import tempfile, os
    from pathlib import Path
    from xianyu_hunter.web.services.cookie_store import CookieStore

    with tempfile.TemporaryDirectory() as tmpdir:
        with patch("xianyu_hunter.web.services.cookie_store._COOKIE_JSON_DIR", Path(tmpdir)):
            store = CookieStore()
            store._cache = {}

            cookies_a = [{"name": "unb", "value": "11111111", "domain": ".goofish.com"}]
            cookies_b = [{"name": "unb", "value": "22222222", "domain": ".goofish.com"}]

            store.export_cookies(cookies_a, method="browser", user_id="user_a")
            store.export_cookies(cookies_b, method="browser", user_id="user_b")

            # user_a 的 Cookie 不包含 user_b 的值
            data_a = store._read_json("user_a")
            data_b = store._read_json("user_b")
            assert data_a["cookies"][0]["value"] == "11111111"
            assert data_b["cookies"][0]["value"] == "22222222"
```

- [ ] **6.2 运行集成测试**

Run: `python -m pytest tests/test_mu2_integration.py -v --tb=short`
Expected: PASS — 3 tests passed

- [ ] **6.3 运行 MU1 + MU2 全量回归**

Run: `python -m pytest tests/test_user_manager.py tests/test_cookie_store_multi_user.py tests/test_auth_middleware_mu2.py tests/test_unified_login_mu2.py tests/test_mu2_integration.py -v --tb=short`
Expected: 全部通过

- [ ] **6.4 提交**

```bash
git add tests/test_mu2_integration.py
git commit -m "test(mu2): 端到端集成测试(WEB_TOKEN+session_token共存+Cookie隔离)"
```

---

## Self-Review

### Spec 覆盖
- ✅ 认证中间件改造（auth.py 三路校验 + user_id 注入）→ Task 3
- ✅ CookieStore 扩展 user_id 维度（JSON 分文件 + 缓存分桶）→ Task 1
- ✅ 登录流程改造（identify_or_create + issue_session + export_cookies）→ Task 4
- ✅ auth_helpers 支持 session_token → Task 2
- ✅ 向后兼容（WEB_TOKEN 直通 + default 用户 + 不传 user_id 回退）→ Task 1/3/5
- ✅ 集成测试 → Task 6

### Placeholder 扫描
- 无 TBD/TODO/"implement later"
- 所有步骤包含完整代码
- 所有测试包含具体断言

### 类型一致性
- `export_cookies(cookies, method, user_id)` 签名一致
- `has_valid_cookies(user_id)` 签名一致
- `make_auth_response(data, session_token)` 签名一致
- `verify_session(session_token) -> Optional[str]` 与 MU1 一致
- `identify_or_create(cookies) -> str` 与 MU1 一致
- `issue_session(user_id) -> str` 与 MU1 一致
