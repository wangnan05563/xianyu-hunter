# 模式 P：评估去重验证测试

> 对应 B-REVIEW-317~319（评估去重防护）
> 配置节点：`config.yaml#mode_p_eval_dedup_test`

### 触发关键词
- 测试评估去重 / 验证 eval 去重防护 / 测试 upsert 逻辑
- 评估明细出现重复记录 / 同一商品多条评估

### 步骤 0：加载配置
读取 `config.yaml` 的 `mode_p_eval_dedup_test` 段。禁止硬编码任何参数。

### 步骤 1：静态扫描
1. 扫描 `upsert_eval_event` 函数，验证是否使用部分唯一索引
2. 验证 C-05 迁移脚本是否对已有重复数据做了去重
3. 扫描所有 eval 写入路径，确认均经过 upsert

### 步骤 2：运行时验证
1. 模拟重复评估触发，验证 DB 中不会出现重复记录
2. 验证 upsert 幂等性

### 步骤 3：生成测试报告
输出去重验证报告，包含静态扫描结果与运行时验证结果。
